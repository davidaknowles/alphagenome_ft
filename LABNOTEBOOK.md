# Lab Notebook

## Multi-study adapter comparison

The fine-tuning comparison uses signed double-centered Pearson correlation, (R), for checkpoint selection. For a prediction matrix (Y \in \mathbb{R}^{L \times C}), (L) is the number of genomic loci and (C) is the number of target channels. Predictions and targets are centered over loci and channels before their Pearson correlation is calculated. This is not the global coefficient of determination reported by some earlier runs. Validation now records a mean of each metric across heads so joint ATAC and ribonucleic-acid, RNA, runs do not select checkpoints from only the first head.

The non-ENCODE study panel comprises the Johansen human, macaque, and marmoset basal-ganglia atlas; the Liu Human Development Multiomic Atlas, HDMA; the Zemke 2023 human, macaque, marmoset, and mouse motor-cortex atlas; the Zemke 2024 human hippocampal-aging atlas; and the Mannens first-trimester human-brain atlas, called HDA in earlier experiments. Each comparison trains the same randomly initialized assay heads and compares low-rank adaptation, LoRA, with LoRA plus low-rank convolution adaptation, LoCon, under matched optimizer, split, precision, and early-stopping settings.

Published Zemke ATAC and RNA BigWigs are retained in their native reads-per-kilobase-per-million, RPKM, units. HDA uses its published ATAC signal and published cluster-level RNA counts per million, CPM. Johansen and Liu targets are reconstructed from measured fragments and raw gene counts to avoid nonlinear significance tracks or model-derived tracks. RNA count matrices are aggregated by cell group, normalized to CPM, represented as strand-specific union-exon density, and paired with direct gene-level supervision.

The Liu audit found 817,740 uniquely identified metadata cells across 76 represented libraries, all of which matched a raw RNA barcode exactly. The paired target panel uses the 186 clusters retained in the source study's published ChromBPNet panel, containing 802,658 cells. Seventeen annotated clusters outside that panel are excluded from both ATAC and RNA and remain listed in the generated audit manifest. Liu ATAC uses all selected paired-end fragments, without downsampling, and normalizes mean 100 bp fragment coverage by the complete cluster library size in millions.

The initial exon-plus-gene Liu LoRA epoch reached validation and test signed double-centered (R=0.7648) and (R=0.7786) for ATAC and (R=0.4053) and (R=0.4390) for RNA. The model and AdamW optimizer state were checkpointed at global step 2,619. This run is no longer canonical because the derived exon-coverage term is omitted from the corrected direct-gene objective. Corrected LoRA and LoRA plus LoCon runs must complete the same epoch before this becomes an adapter comparison.

Sample-level Liu RNA aggregates permit a direct reliability audit. Biological samples are greedily divided into two library-depth-balanced halves independently for each cell group, each half is normalized to counts per million, CPM, and signed double-centered correlation is calculated across groups and genes. Raw CPM has split-half (R=0.8969) across 175 groups estimable in both halves, corresponding to a Spearman-Brown complete-pseudobulk reliability estimate of 0.9457. In contrast, log1p CPM has split-half (R=0.5343) and estimated full reliability 0.6965. The current gene objective is log1p mean-squared error, whereas checkpoint selection uses raw-CPM signed double-centered correlation. The target is therefore reliable enough for (R\approx0.8), but the optimization loss emphasizes a less reproducible representation and is misaligned with the reported metric. Gene-only RNA screens with an explicit raw-CPM signed-correlation term test this explanation without rescaling or discarding measured counts. In data-parallel runs, the correlation objective now reduces global track sums, counts, covariance, and variances across devices. This is algebraically equivalent to double centering the concatenated global batch, while avoiding an all-gather of prediction and target tensors. Forced two-device tests verify both the loss and the replicated-parameter gradient against direct concatenation. Full reliability results are in `results/v0data_liu_rna_split_half_reliability.md`.

New genomic heads normally use Haiku random initialization even though AlphaGenome contains trained heads for the same assays. The optional pretrained bootstrap initializer assigns every new channel to a deterministic empirical channel from the corresponding pretrained ATAC or RNA head. It excludes padding, matches strand where possible, and uses the same source assignment for 1 bp and 128 bp weights, biases, and learned scales. Matching positive- and negative-strand pools are shuffled with the same seed, preserving paired source assays. The resulting channels remain fully trainable and distinct, while their initial directions lie on AlphaGenome's learned output-head distribution. Canonical comparisons retain random initialization; matched HDA smokes and one-epoch screens test the bootstrap initializer as a scientific variant.

Every retained Liu cluster passes the Johansen-derived 500,000-fragment quality threshold. Whole-genome depth ranges from 1,458,794 to 312,508,692 fragments, with a median of 24,587,832. Positive-bin means are negatively associated with depth but also cluster by organ, and no Liu target approaches the 33,424-fragment Johansen failure. The Liu baseline therefore retains all 186 published-panel clusters rather than filtering valid organ-specific accessibility profiles by signal magnitude.

Cross-species batches now carry AlphaGenome organism indices. Human, macaque, and marmoset use the human organism embedding, while mouse uses the model's mouse embedding. Previously, the shared launcher silently assigned the human index to every species. The target heads retain an identical 20-channel ATAC and 20-channel RNA layout across all four Zemke species, and joint runs pool channel-wise RNA nonzero means across species.

The Zemke reference audit checks every modeled chromosome in every ATAC and RNA BigWig against its FASTA length. Published marmoset tracks use UCSC `calJac4`, including `chr1` length 217,961,735 bp. They do not match the newer `mCalJa1.2` reference used by the Johansen atlas, where `chr1` is 216,975,769 bp, or the older `calJac3` assembly, where `chr1` is 210,400,635 bp. Zemke experiments therefore materialize `calJac4` separately. Human, macaque, marmoset, and mouse training is restricted to canonical autosomes and `chrX`; `chr8` and `chr9` are held out for validation and test.

The full-depth Allen ATAC baseline reached its best validation signed double-centered (R=0.4165) at epoch 6, with test (R=0.4581) from the same epoch. Validation did not improve over the next three epochs, so patience-three early stopping ended the run after epoch 9. Reconstructing all-fragment paired-fragment coverage therefore improves substantially over the released-track baseline, whose best validation and test values were (R=0.1894) and (R=0.2100), but remains below 0.8. These values must not be compared with the earlier HDA global coefficient of determination near 0.8 because that is a different statistic.

The final Johansen joint comparison replaces released ATAC tracks with this all-fragment coverage SPMR definition for human, macaque, and marmoset while preserving the aligned 48-group RNA targets. Human shards are reused, nonhuman shards are reconstructed against their own assemblies, and only the ATAC heads are replaced in the aligned species configuration.

The first full HDA ATAC epoch reached validation and test (R=0.7716) and (0.7820) with LoRA. LoRA plus LoCon reached (0.7673) and (0.7753). These values demonstrated that HDA can approach the target correlation, but the pair is superseded because adding LoCon changed Haiku's parameter-creation order and therefore changed the random initialization of later shared LoRA matrices and assay heads.

Adapter initialization now reserves a head random-number-generator key before trunk construction and restores it only inside the head scope. Random adapter leaves use local keys derived from their complete module paths, so adding LoCon does not advance the outer Haiku random-number-generator sequence or alter shared LoRA leaves. The paired smoke gate requires bit-identical shared LoRA and head initialization; LoCon's up projection starts at zero, so its added residual is also exactly zero before optimization.

The reserved head key must be consumed only while Haiku initializes parameters. An initial implementation also requested it during `apply`, where the deterministic training path intentionally supplies no random key; this caused the HDA joint and Zemke 2024 comparison jobs to fail with `MissingRNGError` before their first update. Both custom forward paths now enter the reserved random-number-generator context only when `hk.running_init()` is true. A regression test initializes each strategy with a key, applies each with `rng=None`, and confirms exact equality of shared adapter parameters, head parameters, and initial predictions.

The full AlphaGenome initialization audit passed on the HDA head. All 54 head parameter leaves and 162 shared LoRA parameter leaves were bit-identical between LoRA and LoRA plus LoCon, and all four LoCon up-projection leaves were exactly zero. The paired HDA smoke subsequently produced the same displayed first-step loss, 70.8766, for both strategies. The four-species Zemke smoke differed by less than (10^{-6}) in relative first-step loss, which is consistent with separate XLA graphs evaluating an initially zero residual.

In the first full fair HDA epoch, LoRA reached validation and test signed double-centered (R=0.7715) and (R=0.7819). LoRA plus LoCon reached (R=0.7696) and (R=0.7783). The runs continued because these are epoch-one observations rather than early-stopped model selections; LoRA led validation by 0.0019 at this point.

At HDA epoch 2, LoRA reached validation and test (R=0.7905) and (R=0.8024), while LoRA plus LoCon reached (R=0.7981) and (R=0.8067). LoRA plus LoCon therefore led validation by 0.0076 after starting from exactly matched shared parameters. Both runs continued under the same early-stopping rule.

At HDA epoch 3, LoRA reached validation and test (R=0.7971) and (R=0.8081), while LoRA plus LoCon reached (R=0.8062) and (R=0.8151). The LoRA plus LoCon validation advantage increased to 0.0091. Both runs then continued from their epoch-3 parameters under track-major target loading; optimizer moments restart symmetrically at this continuation boundary.

At HDA epoch 4, LoRA reached validation and test (R=0.8053) and (R=0.8159), while LoRA plus LoCon reached (R=0.8086) and (R=0.8166). Both strategies therefore met the 0.8 validation target for ATAC. The LoRA plus LoCon validation advantage narrowed to 0.0034, and the ATAC-only runs were stopped after saving this epoch so compute could be reassigned to the weaker RNA objectives.

The first paired HDA epoch gave LoRA validation and test (R=0.7722) and (R=0.7830) for ATAC and (R=0.4361) and (R=0.5720) for gene-supervised RNA. LoRA plus LoCon gave (R=0.7729) and (R=0.7837) for ATAC and (R=0.4313) and (R=0.5713) for RNA. ATAC is close to the single-head trajectory, while RNA remains well below the target range and requires further epochs or an RNA-specific objective improvement.

At paired HDA epoch 2, LoRA reached validation and test (R=0.7892) and (R=0.7990) for ATAC and (R=0.5201) and (R=0.6182) for RNA. LoRA plus LoCon reached (R=0.7957) and (R=0.8049) for ATAC and (R=0.5241) and (R=0.6222) for RNA. LoRA plus LoCon led validation by 0.0065 for ATAC and 0.0040 for RNA. RNA is improving substantially with additional training but remains well below 0.8, motivating a full-data screen that augments the log-count objective with signed double-centered correlation loss.

At paired HDA LoRA epoch 3, validation and test reached \(R=0.7947\) and \(R=0.8061\) for ATAC and \(R=0.5170\) and \(R=0.6181\) for RNA. ATAC improved over epoch 2, RNA validation declined by 0.0031, and the mean validation correlation increased by only 0.0012. The checkpoint was preserved and the continuation was stopped before epoch 4 so the missing reconstructed-study baselines could run. The matched LoRA plus LoCon trajectory remains at epoch 2 and must reach epoch 3 before this small mean difference is interpreted as an adapter comparison.

The first human Zemke 2023 epoch gave LoRA validation and test (R=0.6760) and (R=0.6930) for ATAC and (R=0.3963) and (R=0.3475) for RNA. LoRA plus LoCon gave (R=0.6784) and (R=0.6939) for ATAC and (R=0.3999) and (R=0.3462) for RNA. LoRA plus LoCon led both validation heads slightly, but neither RNA result is near the target range after one epoch.

Human Zemke 2023 LoRA continued to epoch 4, reaching validation and test (R=0.6957) and (R=0.7112) for ATAC and (R=0.4809) and (R=0.4051) for RNA. The mean validation selection metric continued to improve, but RNA remained below 0.5 and the run was stopped after checkpointing so the LoRA plus LoCon continuation could reach the same epoch. Later epochs must not be used for a strategy comparison until that matched result exists.

At matched human Zemke 2023 epoch 2, LoRA validation and test reached (R=0.6910) and (R=0.7083) for ATAC and (R=0.4561) and (R=0.3927) for RNA. LoRA plus LoCon reached (R=0.6925) and (R=0.7093) for ATAC and (R=0.4578) and (R=0.3873) for RNA. LoRA plus LoCon led validation by less than 0.002 for both heads and test ATAC by 0.001, while LoRA led test RNA by 0.0054. The adapter comparison remains effectively tied at this depth, and LoRA plus LoCon continues to the existing LoRA epoch-four checkpoint.

At matched human Zemke 2023 epoch 3, LoRA validation and test reached (R=0.6864) and (R=0.7054) for ATAC and (R=0.4784) and (R=0.4009) for RNA. LoRA plus LoCon reached (R=0.6960) and (R=0.7143) for ATAC and (R=0.4765) and (R=0.4011) for RNA. LoRA plus LoCon led ATAC by approximately 0.009 and test RNA by 0.0002, while LoRA led validation RNA by 0.0019. The comparison remains mixed and close; LoRA plus LoCon continues through the matched fourth epoch.

At matched human Zemke 2023 epoch 4, LoRA validation and test reached (R=0.6957) and (R=0.7112) for ATAC and (R=0.4809) and (R=0.4051) for RNA. LoRA plus LoCon reached (R=0.6971) and (R=0.7151) for ATAC and (R=0.4987) and (R=0.4099) for RNA. LoRA plus LoCon led all four values, with a validation RNA gain of 0.0178 and smaller gains elsewhere. The LoRA plus LoCon process was stopped after checkpointing and replaced by a continuation behind the unstarted dataset baselines; RNA remains below the target range.

The first matched macaque Zemke 2023 epoch showed a large chromosome split effect shared by both strategies. LoRA validation and test reached \(R=0.2000\) and \(R=0.5819\) for ATAC and \(R=0.2401\) and \(R=0.2863\) for RNA. LoRA plus LoCon reached \(R=0.2054\) and \(R=0.5871\) for ATAC and \(R=0.2371\) and \(R=0.2839\) for RNA. The strategies differ by at most 0.0054, whereas the ATAC difference between the chr8 validation split and chr9 test split is approximately 0.38 for either strategy. This is a split-specific dataset or sequence-transfer effect rather than evidence for or against LoCon. A matched target-only chr8 and chr9 differential-signal audit is required before deeper macaque optimization.

The matched target-only audit rules out absent chr8 differential signal as the main explanation. For ATAC, chr8 and chr9 double-centered standard deviations are 5.125 and 6.565, and the fractions of total variance retained after double centering are 0.265 and 0.314. Chr8 ATAC contrast is therefore 22% smaller but remains substantial. For RNA, chr8 and chr9 double-centered standard deviations are 4.398 and 4.203, while retained variance fractions are 0.547 and 0.382. Chr8 RNA has slightly more absolute differential variation despite its lower model correlation. The approximately 0.38 ATAC prediction-correlation gap is much larger than the target-statistic differences and appears for both adapter strategies. Further diagnosis should test macaque sequence quality and chromosome-specific transfer from the human-pretrained backbone rather than rescaling chr8 targets.

FASTA composition also rules out gross chr8 assembly quality. Chr8 and chr9 contain 1.459% and 1.555% `N` bases, 35.57% and 34.92% soft-masked bases, and no unexpected sequence symbols. Their canonical-base fractions are 98.54% and 98.44%. The macaque split discrepancy is therefore not explained by missing sequence or differential target variation. Because the comparison uses one chromosome for validation and one for test, deeper macaque conclusions require an alternate held-out chromosome fold to quantify chromosome-to-chromosome variance; selecting a more favorable chromosome without that fold would bias the adapter comparison.

The Zemke species launcher now accepts explicit validation and test chromosome sets. A matched macaque LoRA and LoRA plus LoCon fold with chr10 validation and chr11 test is queued at low priority. This fold retrains after removing both chromosomes from training; evaluating the existing chr8/chr9 checkpoint on chr10 or chr11 would leak training loci and is not a valid estimate of chromosome variance.

The first matched marmoset Zemke 2023 epoch gave LoRA validation and test (R=0.5754) and (R=0.5495) for ATAC and (R=0.2771) and (R=0.1549) for RNA. LoRA plus LoCon gave (R=0.5799) and (R=0.5527) for ATAC and (R=0.2800) and (R=0.1918) for RNA. LoRA plus LoCon led all four values, but the small differences are provisional and both RNA results remain far below the target range. The runs were stopped after the matched checkpoint so mouse training could begin; deeper marmoset continuation remains part of the optimization stage.

The first matched mouse Zemke 2023 epoch gave LoRA validation and test (R=0.2855) and (R=0.2911) for ATAC and (R=0.0053) and (R=0.0157) for RNA. LoRA plus LoCon gave (R=0.3237) and (R=0.3135) for ATAC and (R=0.0009) and (R=-0.0014) for RNA. LoRA plus LoCon improved ATAC, but neither strategy had learned the mouse RNA contrast after one epoch. Both runs were checkpointed and replaced by explicit continuations so the missing Johansen and Liu baseline smokes could use the released accelerators first.

A differential-signal audit sampled 32 windows per Zemke species and represented each 131,072-base-pair window as 1,024 genomic bins. For RNA, the fraction of total variance retained after double centering across genomic bins and 20 cell types was 0.5806 for human, 0.3081 for macaque, 0.3578 for marmoset, and 0.4197 for mouse. Mouse RNA had a nonzero fraction of 0.2607 and a double-centered standard deviation of 5.0678, which were also within the cross-species range. The near-zero mouse RNA prediction correlation therefore cannot be attributed to absent differential target signal or exceptional sparsity. The shared four-species head provides the next test of whether transfer from the three primates resolves the optimization failure.

The first pooled four-species Zemke epoch gave LoRA validation and test (R=0.4017) and (R=0.6478) for ATAC and (R=0.2950) and (R=0.1990) for RNA. LoRA plus LoCon gave (R=0.4333) and (R=0.6540) for ATAC and (R=0.2835) and (R=0.2152) for RNA. LoRA plus LoCon led ATAC and test RNA, while LoRA led validation RNA. These metrics aggregate observations across species and must not be interpreted as mouse-specific transfer evidence. Both strategies were checkpointed, moved to queued continuations, and scheduled for evaluation against each species' native manifest.

The first matched Zemke 2024 epoch gave LoRA validation and test (R=0.7306) and (R=0.7406) for ATAC and (R=0.3904) and (R=0.2319) for RNA. LoRA plus LoCon gave (R=0.7344) and (R=0.7457) for ATAC and (R=0.3879) and (R=0.2364) for RNA. LoRA plus LoCon led ATAC and test RNA slightly, while LoRA led validation RNA by 0.0025; these mixed differences do not establish an adapter advantage. As in human Zemke 2023, RNA is much weaker than ATAC and provides a second test of whether optimizing transformed count likelihood alone is misaligned with the raw signed-correlation selection metric.

At matched Zemke 2024 epoch 2, LoRA validation and test reached (R=0.7424) and (R=0.7488) for ATAC and (R=0.4326) and (R=0.2535) for RNA. LoRA plus LoCon reached (R=0.7441) and (R=0.7508) for ATAC and (R=0.4288) and (R=0.2506) for RNA. LoRA plus LoCon led ATAC by less than 0.002, while LoRA led RNA by approximately 0.003; the strategy result remains effectively tied. Both heads continued improving, so LoRA continues to the existing LoRA plus LoCon best epoch at four.

At matched Zemke 2024 epoch 3, LoRA validation and test reached (R=0.7439) and (R=0.7548) for ATAC and (R=0.4713) and (R=0.2775) for RNA. LoRA plus LoCon reached (R=0.7483) and (R=0.7601) for ATAC and (R=0.4614) and (R=0.2843) for RNA. LoRA had the higher mean validation result because its RNA correlation led by 0.0099, while LoRA plus LoCon led ATAC and both test heads by 0.0045 to 0.0067. The mixed result still does not establish a consistent adapter advantage, and LoRA continues through matched epoch 4.

At matched Zemke 2024 epoch 4, LoRA validation and test reached (R=0.7459) and (R=0.7560) for ATAC and (R=0.4706) and (R=0.2739) for RNA. LoRA plus LoCon reached (R=0.7499) and (R=0.7599) for ATAC and (R=0.4722) and (R=0.2888) for RNA. LoRA plus LoCon led all four values, with small ATAC gains of approximately 0.004 and a larger test RNA gain of 0.0149. This is the first Zemke 2024 matched epoch with a consistent LoCon advantage, although RNA remains well below the target range.

At matched Zemke 2024 epoch 5, LoRA validation and test reached (R=0.7436) and (R=0.7534) for ATAC and (R=0.4759) and (R=0.2925) for RNA. LoRA plus LoCon reached (R=0.7462) and (R=0.7543) for ATAC and (R=0.4708) and (R=0.2945) for RNA. LoRA had the slightly higher mean validation score because of its RNA lead, while LoRA plus LoCon led validation ATAC and both test heads. The result returns to a mixed strategy comparison; the LoRA process was checkpointed and moved behind the missing dataset baselines.

By Zemke 2024 LoRA plus LoCon epoch 3, validation and test reached (R=0.7483) and (R=0.7601) for ATAC and (R=0.4614) and (R=0.2843) for RNA. RNA again improved with each epoch but remained substantially weaker than ATAC. The missing LoRA baseline was rescheduled after an underperforming worker was detected and remains required before drawing a strategy conclusion.

Zemke 2024 LoRA plus LoCon selected epoch 4 over epoch 5. Epoch 4 validation and test were (R=0.7499) and (R=0.7599) for ATAC and (R=0.4722) and (R=0.2888) for RNA. Epoch 5 validation declined to (R=0.7462) for ATAC and (R=0.4708) for RNA. The run was stopped after writing the epoch-5 checkpoint so the matched LoRA trajectory could catch up and the missing human Zemke LoRA plus LoCon continuation could start.

The HDA RNA objective reads 268 strand-specific exon pseudo-coverage tracks per batch even though checkpoint selection and RNA evaluation use direct gene-level CPM. A gene-only screen sets the auxiliary coverage weight to zero, requests only the 128 bp RNA output, and omits those BigWig reads and the corresponding coverage loss. The RNA head still uses the same track metadata and predicts the same 134 cell-group channels; exon weights aggregate its 128 bp output over each fully contained gene, and direct gene targets and metrics are unchanged. Batch conversion now treats coverage tensors as optional when a head has direct gene supervision, matching the data module and objective contract. Slurm launchers pass the allocated local GPU count to JAX, so a global batch of eight is sharded to four examples per device on two-GPU L40S runs instead of being placed on one device. Matched LoRA and LoRA plus LoCon screens compare gene-only log-count loss with correlation weights 0, 0.1, and 1. This separates objective alignment from target normalization while reducing avoidable input and output work.

The initial full-data correlation-loss screens became non-finite at update 100 for weights 0.1, 1, and 10. The forward loss guarded a zero denominator only after computing its square root, so a batch with zero double-centered variance could still create an infinite derivative and NaN gradients. The corrected implementation floors the product of prediction and target sums of squares before the square root, then returns zero only for the no-variance case. A regression test differentiates through constant prediction and target matrices and requires a finite zero gradient. The failed screens produced no checkpoint or metric and are excluded from comparisons.

The same stabilized objective applies to heads without gene supervision. It selects the highest output resolution compatible with the genomic target, pools the target when necessary, and computes signed double-centered correlation across genomic observations and tracks. The single-study and Zemke species launchers now expose the existing optional correlation weight used by the joint launcher. Baselines leave it unset; any correlation-weighted optimization run uses a distinct checkpoint suffix and must first pass a full-data smoke test.

Published Zemke RNA is consistently weaker than ATAC, so a global correlation-loss override unnecessarily changes both objectives. A copied-manifest utility can now set the outer loss weight and the signed double-centered correlation weight for one named head. RNA-only correlation screens retain the canonical ATAC likelihood and leave source manifests and BigWigs unchanged.

The full-data Allen ATAC correlation-loss screen completed one matched epoch with weights 1, 10, and 100. The unweighted epoch-one baseline reached validation and test \(R=0.4011\) and \(R=0.4450\). Weights 1, 10, and 100 reached \(R=0.4045/0.4484\), \(R=0.4063/0.4500\), and \(R=0.4067/0.4502\), respectively. Direct metric alignment therefore gives a reproducible gain of approximately 0.005, but the improvement saturates by weight 10 and remains far below the 0.8 target. Larger weights are not justified; target composition and transferable biological signal remain the dominant limitations for Allen.

Joint heads previously entered the optimization objective with equal scalar weights even when their native likelihood scales differed substantially. At Zemke 2024 epoch 1, validation losses were approximately 150 for ATAC and 20 for RNA, so the summed gradient could be dominated by ATAC while checkpoint selection gave both correlations equal weight. Target manifests now support a positive outer `loss_weight` per head, defaulting to 1. It multiplies the complete head objective only in the training sum and leaves the reported per-head loss unchanged. This differs from the inner gene and exon-coverage weights within a gene-supervised RNA head. RNA-weight screens use copied manifests and distinct checkpoint suffixes; canonical baselines remain unchanged.

An optional first-batch gradient diagnostic measures each head's unweighted L2 gradient norm on shared LoRA and LoCon parameters and on its own head parameters. It also reports the configured-weight-adjusted shared norm and the pairwise cosine between heads' shared-adapter gradients. This provides a direct basis for loss balancing because raw likelihood values alone do not determine their relative influence on shared adapter updates.

Evaluation-only runs load head and adapter parameters, execute requested splits, and write a separate evaluation record without optimizer updates or checkpoint writes. The four-species Zemke checkpoints are evaluated against each species' native reference and target manifest because the joint training loop's aggregate validation result pools observations across species.

Johansen joint checkpoints likewise require separate human, macaque, and marmoset evaluation. The runner can now select one data module from a multi-species configuration during evaluation while retaining the joint model's pooled track-scaling metadata. This avoids rebuilding a numerically different species-specific head and provides per-species evidence in addition to the pooled training metric.

The first 14,529-step Johansen epoch exposed a post-training synchronization cost in metric accumulation. Each loss and sufficient-statistic leaf had previously been added with an independent host-dispatched JAX operation, allowing hundreds of thousands of small additions to queue behind model updates and drain only at epoch end. Epoch statistics are now accumulated by one pmapped tree addition per optimizer step. This leaves metric definitions and model updates unchanged, reduces dispatch count, and keeps the accumulation dependency on device. A two-step training regression test covers the fused path and exact one-time gradient diagnostic behavior.

The first full Johansen LoRA plus LoCon epoch reached validation and test signed double-centered (R=0.5107) and (R=0.4704) for ATAC and (R=0.2721) and (R=0.3621) for direct gene-level RNA. These are pooled human, macaque, and marmoset metrics; native-species evaluations are dependency-gated on the saved best checkpoint. The process was stopped after checkpointing so the missing matched LoRA epoch could use the released accelerator. One subsequently dispatched epoch-two update was discarded. The queued continuation restores the epoch-one `last` parameters and AdamW state and will use fused metric accumulation, so its epoch-two trajectory begins from the documented boundary rather than the discarded update.

A matched human-only Johansen baseline is queued on the same full-depth reconstructed human ATAC and direct gene-expression targets used by joint training. It retains the pooled channel scaling and chr8/chr9 validation/test holdouts, changing only species sampling. Comparing it with the human native-species evaluation of the joint checkpoint will distinguish human target and objective limitations from negative transfer across primates.

Final adapter comparisons are collated from each run's `metrics.jsonl`. One epoch is selected per run by the mean validation signed double-centered Pearson correlation across all heads, and every per-head validation and test value is reported from that same epoch. This preserves the joint checkpoint-selection contract and avoids selecting separate favorable epochs for ATAC and RNA.

The canonical coverage audit distinguishes a matched epoch from a completed optimization trajectory. HDA ATAC-only, human, macaque, marmoset, mouse, pooled four-species Zemke 2023, and Zemke 2024 have at least one matched LoRA and LoRA plus LoCon epoch. HDA joint has a matched epoch 2 and an additional LoRA epoch 3. Filtered Johansen and Liu remain the only canonical studies without a completed epoch from either strategy; both have active or pending production processes. This audit does not label a run complete merely because one matched epoch exists.

The Liu preparation keeps its target manifest with the generated exon RNA tracks and gene-level supervision under `outputs/v0data/liu-hdma/joint`. The generic joint launcher resolves this dataset-specific layout automatically; without that mapping it would look for a nonexistent parent-level manifest and fail before model construction.

Johansen reprocessing launchers query species configurations with Python's structured JSON parser rather than assuming that the `jq` command-line executable is installed on every worker image. The query is shared by chromosome-job enumeration and FASTA lookup. Its launch-time check requires exactly 44 macaque and marmoset chromosome jobs before any fragment processing begins.

Johansen fragment cell identifiers require species-specific joins. All 90 macaque fragment files contain library-local 10x barcodes; barcode overlap assigns every file to a distinct metadata library with 100% coverage and recovers 58 groups. Marmoset has 112 biological libraries stored twice, once as a full fragment file and once as an H5AD-filtered exact subset. For overlapping cells, the subset and full resource have identical whole-genome fragment counts. Reprocessing uses only the 112 filtered copies so retained cells are counted once; processing all 224 resource directories would duplicate their fragments.

All Johansen chromosome shards and the three species' ATAC BigWigs completed. The first target-assembly job failed before running Python because its activated Python 3.12 executable could not find the matching shared runtime library. The target launcher now exports the same runtime-library path as the successful materialization launcher. The lightweight assembly step then completed and validated three species with 48 ATAC and 96 strand-specific RNA channels per species. Fresh dependent smoke and full adapter arrays were submitted; the failed dependency chain contains no model result and is not part of the comparison.

Depth auditing found that the shared Johansen group `SN GATA3-PVALB GABA` had only 33,424 whole-genome fragments in human, compared with a human group median of 109,760,310 and 77,385,705 and 13,656,984 fragments for the same group in macaque and marmoset. Its human positive-bin ATAC mean was 18.09, versus a median of 0.068, confirming that library-size normalization amplified an exceptionally sparse and noisy target. This one track contributes 47.5% of the genome-wide sum of squared ATAC signal across all 48 human channels and 59.7 times the median track sum of squares, so retaining it would make the aggregate raw double-centered metric largely a test of one underpowered pseudobulk. The comparison now requires at least 500,000 ATAC fragments in every species and excludes a failing group synchronously from all ATAC, strand-specific RNA, and gene-expression targets. This retains 47 aligned groups; the next-lowest human group has 938,023 fragments. The original 48-group artifacts remain preserved separately.

The original Johansen RNA preparation used the March `pseudobulk_aligned` matrices, which were derived from group pseudobulks that sum expression after per-cell normalization and only then renormalize each cell-group row to CPM. Corrected artifacts instead sum raw unique molecular identifier, UMI, counts within each group before CPM. The normalization audit compares the corresponding pre-alignment group matrices with identical group and gene labels. Even after both versions are put in CPM units, raw double-centered agreement is only (R=0.4645) for human, (R=0.5023) for macaque, and (R=0.5430) for marmoset; log1p agreement is (R=0.9224), (R=0.9418), and (R=0.9537), respectively. This changes the raw cell-group-specific structure used by the reported metric, not merely target scale. Previous Johansen RNA fine-tuning results are therefore superseded. The corrected pipeline aligns all three species to the existing genomic supervision annotations and tests direct gene supervision before regenerating derived exon coverage. The 30 GB marmoset H5AD stores 313,033 cells by 35,787 genes with 1.13 billion nonzero raw counts. Its `raw/X` values are integer-valued and agree with `layers/UMIs` on checked rows, whereas normalized `X` is distinct. Reprocessing reads `raw/X` directly in compressed-sparse-row chunks and accumulates a dense group-by-gene matrix, avoiding eager loading of the full matrix or unrelated layers while retaining every count.

The first filtered species configuration correctly contained 47 ATAC and 94 strand-specific RNA tracks but inherited stale top-level and per-species counts of 48 from its source configuration. Preparation now derives `shared_groups` and every summary count from the retained group list. Regeneration reports 47 consistently without changing any target values. Gene-only Liu and Johansen variants set exon coverage weight to zero, request only the 128 bp RNA output, and retain direct counts-per-million gene supervision. These variants avoid reading 372 Liu or 94 Johansen synthetic exon-density tracks per species. Matched smoke screens are queued behind the canonical exon-plus-gene baselines, with dependent full-data comparisons that are released only if both adapter strategies pass their gate.

The reconstructed-study RNA screen is factorial at the manifest level. In addition to canonical exon-plus-gene supervision and plain gene-only supervision, Liu and Johansen each have a gene-only variant with correlation weight 0.1. The latter adds the stabilized signed double-centered objective only to the RNA head. Matched LoRA and LoRA plus LoCon smoke gates precede every full run, and all optimization variants remain lower priority than the canonical strategy pairs.

BigWig loading now assigns each target worker one track handle and reads that track across the batch before transposing the result to window-major layout. The earlier nested window and target pools divided the available reader budget by the number of outer workers, and multiple outer workers could submit reads against the same BigWig handle. Track-major loading uses all configured readers without concurrent access to one handle. This is important for the pending Johansen and Liu joint panels, which contain substantially more channels than the 40-track Zemke 2023 panel.

The first macaque Zemke pair was limited by a transient worker-storage bottleneck rather than target size or model compute. An identical 200-window read from the macaque BigWig took 3.116 seconds on its assigned worker and 0.430 seconds on another active worker, while the macaque GPU was idle despite holding the compiled model. Macaque has 40 tracks totaling 13.2 GB, less than the corresponding mouse panel, so dataset volume does not explain the 7.2-fold read difference. A second comparison using an HDA track differed by 18.8-fold between the same workers, confirming a worker-level storage problem. Neither macaque strategy had reached an epoch checkpoint; both tasks were replaced so replaying 500 updates would cost less than completing the input-bound epoch. HDA continuations that were subsequently assigned to the same worker were also replaced before their first update, preserving their existing epoch-two checkpoints.

The human Zemke pair completed epoch 1 under the earlier loader, then continued from the saved head and adapter parameters under track-major loading. Continuation preserves metric history, epoch numbering, deterministic epoch shuffle seeds, and early-stopping state. Those parameter-only checkpoints did not contain optimizer moments, so Adam restarted at the continuation boundary. Both adapter strategies used the same boundary and optimizer reset; this was a throughput intervention rather than a strategy-specific change.

An end-to-end continuation smoke advanced an HDA checkpoint from epoch 1 and global step 1 to epoch 2 and global step 2, then wrote both the `last` and metric-history artifacts with the continued numbering. This verifies parameter restoration and bookkeeping independently of the longer production continuations. That smoke used the earlier parameter-only checkpoint format, so optimizer-state equivalence to an uninterrupted run was out of scope.

New `best` and `last` checkpoints serialize the masked AdamW state alongside model parameters and restore it against the freshly initialized optimizer tree. A round-trip test advances AdamW once, saves nonzero moments with Orbax, restores into a matching target tree, and requires exact equality of every state leaf. Older checkpoints without this artifact continue to load with an explicit fresh-state message. Thus active runs and already queued continuations remain backward compatible, while checkpoints they write can continue without another optimizer reset.

The first four-species Zemke processes had imported the earlier nested-loader implementation before the track-major change landed. After compilation, neither strategy reached its first 100-update progress interval in more than 12 minutes, whereas corrected-loader study runs advanced by hundreds of updates over the same interval. Those joint processes were replaced before completing an epoch, so no model-selection result or reusable checkpoint was discarded.

The JAX command-line option `--base-compute-dtype` originally controlled only LoRA-patched base projections; AlphaGenome's complete convolution, attention, and decoder trunk remained under a hard-coded bfloat16 mixed-precision policy. The training API now exposes `--backbone-compute-dtype` separately and applies it during fresh initialization and checkpoint restoration. AlphaGenome also explicitly requests the `BF16_BF16_F32` dot algorithm in attention and splice-junction einsums, so non-bfloat16 execution uses a scoped compatibility path that lets JAX select a supported dot algorithm. The default and all matched production comparisons remain bfloat16, so their numerics are unchanged.

V100 diagnostics established the compatibility boundary. A float32 backbone completed full model and adapter initialization and reached the first backward pass, while float16 compute and frozen-parameter storage did the same. Both then exceeded the 32 GB device capacity during 131 kb adapter backpropagation. Thus the former V100 failure was first an unsupported hard-coded bfloat16 algorithm and then a genuine training-memory limit; end-to-end Johansen and Liu smoke tests require the production accelerator class or a separate rematerialized training implementation.

The v0data launchers now prepend the Python 3.12 shared-library directory to `LD_LIBRARY_PATH`. Blackwell workers had discovered this runtime implicitly, but L40S workers exited before Python startup with a missing `libpython3.12.so.1.0`. After applying the explicit runtime path, matched LoRA and LoRA plus LoCon smoke tests completed for all 186 Liu ATAC and RNA groups and all 47 filtered Johansen groups. Both canonical production arrays then reached finite first updates. Liu batch size 8 compiled at approximately 66 GB of device memory, below the 96 GB capacity used for the production comparison.

The target differential-signal audit accepts an explicit worker count and reads independent BigWig tracks concurrently while preserving target order and deterministic sampled windows. Synthetic and real-track checks require exact equality between one-reader and multi-reader results. Eight readers reduced a small cached 16-track check from 8.0 to 3.6 seconds, but did not improve the full remote 402-track HDA audit because the storage backend became saturated. The command therefore defaults to one reader; modest concurrency should be selected per manifest only after measuring the storage path.

The canonical Liu objective materializes 585,105,408 float32 target values for each batch of eight 131,072 bp windows, 2.34 GB comprising 186 ATAC and 372 strand-specific synthetic exon RNA channels. Device utilization occurs in short bursts separated by host preparation, but two exact loader alternatives did not improve the full batch path. Reconstructing values from sparse stored BigWig intervals produced the same checksum as dense decoding but took 5.886 seconds rather than 3.639 seconds because Python interval expansion outweighed the reduced BigWig decoding. Preallocating the final target tensor to avoid two host copies also preserved the checksum but took 3.639 seconds versus 3.643 seconds for the existing assembler. Neither implementation is retained. The useful reduction is objective-level, the queued gene-only RNA variants omit synthetic exon coverage while preserving direct counts-per-million gene supervision and the 128 bp RNA prediction path.

Target-signal audits now average exact per-base values after replacing uncovered BigWig bases with zero, matching training semantics. BigWig summary means can instead average only covered bases and may use file-specific zoom levels, which makes sparse tracks and independently generated files incomparable. A partial-bin regression test distinguishes these definitions. The selector also recognizes RefSeq `NC_` chromosome accessions so macaque targets are audited on their primary assembly records.

Under the corrected definition, HDA synthetic exon RNA has double-centered standard deviation 0.000927, below Liu at 0.001412 and Johansen at 0.001231 to 0.001842. HDA nevertheless has better RNA prediction correlation and retains 91.0% of centered variance after double centering, versus 63.1% for Liu and 47.9% to 55.8% for Johansen. The reconstructed-study deficit is therefore not explained by insufficient target magnitude or absent differential variation. Released Zemke RNA is much denser, 26.1% to 31.8% nonzero compared with 1.0% to 3.8% for synthetic exon density, reinforcing direct gene supervision as the primary objective when coordinate-resolved RNA was not measured. The complete table is in `results/v0data_target_signal_comparison.md`.

ATAC pseudobulk depth is strongly associated with normalized target quality. Across Liu and the three Johansen species, Spearman correlation between log10 fragments and nonzero 128 bp coverage is 0.95 to 0.99, and correlation with median similarity to other tracks is 0.48 to 0.62. Log depth correlates negatively with log root mean square signal, -0.15 to -0.42, showing that signal-per-million normalization can amplify shallow tracks without recovering coverage. Liu has 46 of 186 groups below 10 million fragments; a synchronized 10-million filter retains 140 ATAC groups, 280 strand-specific RNA channels, and 140 gene-expression rows. Applying the threshold in every Johansen species retains 38 shared groups. Matched LoRA and LoRA plus LoCon smoke-gated screens combine this filter with gene-only RNA, with and without correlation weight 0.1. They remain low priority behind canonical comparisons.

The ATAC depth threshold does not leave a separate severe RNA-depth tail. Across Liu groups, ATAC fragment and RNA unique-molecular-identifier depths have Spearman correlation 0.79. The analogous ATAC-to-expression-depth correlations are 0.96, 0.99, and 1.00 for Johansen human, macaque, and marmoset. Every one of the 140 retained Liu groups has at least 1.68 million RNA counts, 138 have at least two million, and 136 contain at least 1,000 cells. Among the 38 retained Johansen groups, minimum RNA cell counts are 159 in human, 286 in macaque, and 220 in marmoset. A second RNA threshold would therefore remove few additional groups and is not included in the first factorial comparison. Per-group evidence is in `results/v0data_rna_pseudobulk_depth.json`.

The canonical manifests use inconsistent internal loss scaling for published ATAC tracks. Reconstructed Liu and Johansen ATAC targets carry per-track nonzero means, while HDA and Zemke ATAC targets default to one; published RNA targets already carry measured nonzero means. AlphaGenome divides each target by this value before its multinomial loss and multiplies predictions by it before raw-unit metrics. Therefore the metadata does not alter the reported signed double-centered correlation, but it changes optimization conditioning and the relative influence of ATAC and RNA. A reversible manifest transformation now computes exact base-weighted means over finite positive BigWig values for selected heads. Separate HDA and Zemke screens will test this normalization without changing canonical manifests or target files. First-batch per-head adapter-gradient diagnostics were also queued for HDA, Liu, Johansen, human and mouse Zemke 2023, and Zemke 2024 so any subsequent head weighting can be based on gradients rather than raw loss magnitudes. Each diagnostic is persisted as structured JSON with its epoch and pre-update global step in addition to being printed.

Canonical adapter results and scientific optimization variants are collated separately. The canonical table accepts only exact dataset-plus-strategy run names. The variant table records preprocessing and objective suffixes such as gene-only RNA, depth filtering, correlation loss, and nonzero-mean scaling, while excluding technical smokes and gradient diagnostics. This preserves fair LoRA versus LoRA plus LoCon comparisons while making one-epoch screens auditable.

## GRR prediction-target audit

Excluding the Encyclopedia of DNA Elements collection, all four single-cell Genomic Resource Repository, GRR, studies contain experimentally measured targets suitable for some form of sequence-model supervision. `johansen2025Crossspecies` has released assay for transposase-accessible chromatin, ATAC, BigWigs and raw fragments, while its RNA measurements are gene-level. `liu2026Multiomics` has raw ATAC fragments and gene-level RNA; its released observed BigWig is a Model-based Analysis of ChIP-Seq version 2 significance transformation, and its other two BigWigs are ChromBPNet predictions and Deep Learning Important FeaTures attributions rather than assay targets. `zemke2023Conserved` has coordinate-resolved ATAC, RNA, high-throughput chromosome conformation capture, and methylation targets. `zemke2024Epigenetic` has coordinate-resolved ATAC and RNA targets, with missing age-by-cell-type combinations that require masks.

`liu2026Multiomics` is the Human Development Multiomic Atlas, HDMA, from Liu et al. It is distinct from the dataset called the Human Developmental Atlas, HDA, in the earlier fine-tuning experiments. HDA is Mannens et al.'s 6 to 13 post-conception-week whole-brain atlas generated with 10x Genomics ATAC and multiome assays. HDMA is a 10 to 23 post-conception-week, 12-organ atlas generated with SHARE-seq. The similar names caused an acronym collision but the studies do not share samples or prediction targets.

The eight reported `zemke2023Conserved` download failures are four-species ATAC and RNA tracks for chandelier cells. Inspection of the WashU source directories showed 20 published tracks per species and no chandelier-cell files under the attempted names. The paper states that low-coverage cell types, including chandelier interneurons, were removed from sequence-model training and evaluation. Local Gene Expression Omnibus accession GSE229169 matrices contain chandelier-cell observations and can produce gene-level RNA and peak-restricted ATAC pseudobulks, but these would not reproduce the missing genome-wide reads-per-kilobase-per-million BigWigs. The compatible resolution is to declare the 20-track ATAC/RNA release complete and remove chandelier cells from required-download checks. The local GRR downloader and README were updated accordingly. Any matrix-derived chandelier targets should be a separately labeled experiment; full track reconstruction would require raw-read reprocessing and remains limited by low cell counts.

## Allen and Human Brain Development ATAC preprocessing

The Allen adult basal ganglia and Human Brain Development, HDA, assay for transposase-accessible chromatin, ATAC, pseudobulks were produced by different pipelines and represent different biological domains. The [HDA source study](https://www.nature.com/articles/s41586-024-07234-1) describes developmental pseudobulks downsampled to 25 million fragments followed by Model-based Analysis of ChIP-Seq version 2, MACS2, signal generation using signal per million reads, SPMR. The [Allen Human Multiome Brain Atlas release](https://brain-map.org/consortia/hmba/hmba-release-basal-ganglia) describes SnapATAC2 processing and supplies group-level BigWigs alongside separately called MACS3 peaks. Inspection of the released files shows a second important implementation difference, HDA BigWig values are constant on native 100 bp bins, whereas Allen values use native 10 bp bins.

This bin width explains the apparent tenfold scale difference in base-expanded values. Across eight matched 131,072 bp windows, the stored HDA and Allen means were 0.480 and 0.0499 and their root mean squares were 5.58 and 0.556. Dividing each value by its native bin width gives means of 0.00480 and 0.00499 per bp and root mean squares of 0.0558 and 0.0556 per bp. Their first two moments are therefore already closely matched when expressed in common units. Differences remain in sparsity, upper tails, cell populations, anatomical region, and developmental stage.

An Allen ATAC-only baseline matches the earlier LoRA setup at 131 kb, batch size 8, learning rate \(10^{-3}\), LoRA without LoCon, float32 frozen and adapter parameters, and bfloat16 activations and compute. Model selection uses signed double-centered Pearson correlation, denoted differential Pearson correlation in the training logs. The raw-target baseline reached its best validation correlation at epoch 7, validation \(R=0.1894\) and test \(R=0.2100\).

Target-transform experiments apply transforms inside the JAX loss and retain raw Allen targets in each batch. Predictions are mapped back to raw Allen units before metrics are accumulated. Spatial averaging is not invertible, so its scale factor is removed but the prediction remains locally smoothed and is compared directly with unsmoothed raw targets. Every one-epoch screen used all 20,952 training windows and the same initialization and optimization settings.

| Optimization target | Validation \(R\) | Test \(R\) |
|---|---:|---:|
| Raw Allen baseline | 0.1787 | 0.1986 |
| Scalar 4x | 0.1699 | 0.1904 |
| Scalar 10x | 0.1609 | 0.1803 |
| Scalar 20x | 0.1534 | 0.1722 |
| Per-track mean match | 0.1559 | 0.1749 |
| Per-track RMS match | 0.1539 | 0.1721 |
| 50% quantile blend toward HDA | 0.1309 | 0.1418 |
| Local 100 bp mean | 0.1869 | 0.2046 |
| Local 100 bp mean, then 10x scale | 0.1728 | 0.1937 |

Increasing target magnitude hurts monotonically, and matching each track's mean or RMS is no better. Quantile normalization is substantially worse because it forces Allen's positive-value tail toward a reference pooled across biologically unmatched HDA tracks. Local 100 bp averaging is the only helpful transform, while multiplying that average by ten removes its advantage. The smoothing result indicates that matching HDA's spatial granularity reduces high-frequency target noise; it does not support a missing normalization factor.

With signed-correlation model selection and patience 3, the 100 bp-smoothed run reached its best validation result at epoch 5, validation \(R=0.1948\) and test \(R=0.2120\). This is a modest gain over the raw baseline, 0.0054 validation and 0.0020 test. It does not explain the much larger gap from the earlier HDA result near \(R=0.8\). The remaining gap is more consistent with differences in cohort, target composition, genomic signal structure, and track count than with scalar preprocessing.

A 128 bp-only output head was also exercised but not retained. It requires a coarse pooled-target metric that is not directly comparable with base-resolution fidelity, and its training path was input-bound and severalfold slower than the base-resolution setup. The run was stopped after confirming finite optimization rather than spending further compute on a dominated strategy.

### Fragment-level reconstruction

The managed Allen collection already contains the full human ATAC fragment export, 204 tabix-indexed five-column fragment files with per-cell whole-genome fragment-count summaries. The accompanying comprehensive H5AD contains 1,034,819 observation identifiers and their 60 `Group` assignments. Fragment barcodes join directly to the H5AD observation index, so reconstructing pseudobulks does not require downloading the source H5AD matrices.

The reconstruction streams each chromosome independently and accumulates native 100 bp targets. Signal per million reads, SPMR, denotes signal normalized by the library size in millions; these paired-end ATAC tracks use one million fragments as the denominator. Shifted-insertion SPMR places the two Tn5 cut sites at fragment start +4 bp and fragment end -5 bp and divides bin counts by the group's whole-genome fragments in millions. Coverage SPMR accumulates exact fragment-covered bases, including partial bins, divides by effective bin width, and then divides by whole-genome fragments in millions.

The full chr8 pilot processed 1,831,505,568 records with no unmatched cell identifiers. Across eight 131 kb windows and all 60 groups, the released tracks had mean 0.0499, root mean square 0.2156, and zero fraction 0.2563. Shifted-insertion SPMR had mean 0.0533, root mean square 0.2013, zero fraction 0.2576, ordinary Pearson (R=0.9619), and double-centered (R=0.9610). Coverage SPMR had mean 0.0495, root mean square 0.1522, zero fraction 0.2041, ordinary Pearson (R=0.6997), and double-centered (R=0.6465). The shifted-insertion definition closely recovers the released signal, while coverage represents a smoother alternative target rather than the release pipeline.

The Human Developmental Atlas, HDA, methods used Model-based Analysis of ChIP-Seq version 2, MACS2, with paired-fragment input, pileup output, and signal-per-million-reads normalization. MACS2 paired-end mode piles up each observed fragment and ignores the read-shift and fixed-extension arguments. The reported 25-million-fragment downsampling balanced pseudobulk replicates for peak calling, but it is not needed to define a library-normalized training signal. The all-data analogue retains every Allen fragment and coordinate multiplicity, computes exact paired-fragment coverage, divides by the complete group fragment count in millions, and averages within 100 bp bins. It has the same expectation as random downsampling followed by normalization and lower sampling variance.

On the chr8 pilot windows, the HDA tracks had mean 0.4796, root mean square 5.5794, zero fraction 0.3258, and 99th percentile 7.5544. Allen full-depth fragment pileup had mean 0.0495, root mean square 0.1522, zero fraction 0.2041, and 99th percentile 0.4937. The target definitions are now conceptually aligned, but their marginal distributions remain different. Earlier scalar and quantile transformations reduced fine-tuning performance, so the full-depth pileup is evaluated without forced marginal matching. Fine-tuning performance, rather than scale alone, determines whether this reprocessing addresses the Allen gap.

## Model and fine-tuning components

AlphaGenome uses a convolutional encoder, transformer tower, decoder, and assay-specific genomic output heads. The current Allen Brain Multiome experiment trains two new heads together with LoRA adapters on linear layers and LoCon adapters on selected convolutions. Frozen backbone parameters are stored in bfloat16, while adapter and head optimization remains compatible with bfloat16 forward compute.

## Allen Brain Multiome v0

The human dataset contains paired cell-type pseudobulks for 60 groups. ATAC supervision is supplied as one unstranded base-resolution BigWig per group. RNA supervision is available as a dense group-by-gene matrix rather than read coverage, so it cannot directly supervise the RNA-seq head at base resolution.

The initial RNA targets used paired positive- and negative-strand pseudo-coverage tracks for every group. For a gene with expression \(c\) CPM and annotated body length \(L\) bp, every base in the gene body received density \(c/L\). Although each gene integral was \(c\), this representation marked introns as expressed and evaluated RNA over every genomic base.

The ATAC and RNA heads contain 60 and 120 channels, respectively. Chromosome holdouts prevent sequence windows from crossing training, validation, and test partitions.

The five-epoch run was stopped during epoch 3 because RNA did not learn a useful signal. At epoch 2, validation RNA global R2 was -0.878 and differential Pearson correlation was 0.0008. Human ATAC did learn, with validation global R2 0.033, R2 over loci 0.378, and differential Pearson correlation 0.106. The contrast indicated a problem with RNA target representation and evaluation rather than a general failure of LoRA and LoCon training.

## Allen Brain Multiome v1 exon supervision

The revised representation merges all annotated exons for each stable Ensembl gene identifier. A gene with union-exon length \(L_e\) bp and expression \(c\) CPM contributes density \(c/L_e\) only to its exonic bases. Overlapping genes add within a strand and opposite strands remain separate. The integral of an isolated gene's pseudo-coverage remains \(c\), introns are zero, and track-specific nonzero means are passed to the AlphaGenome RNA head for target scaling. Preparation matched 36,474 of 36,601 expression genes, 99.65%.

RNA also receives direct gene-level supervision from the 128 bp output. For batch size \(B\), sequence-bin count \(S_{128}=S/128\), at most \(G\) genes per window, and \(C=60\) cell groups, exon weights have dimension \([B,S_{128},G]\), gene targets have dimension \([B,G,C]\), and the validity mask has dimension \([B,G]\). Each weight is the fraction of a 128 bp bin covered by a gene's union exons. Positive- and negative-strand predictions are separated from the interleaved 120 RNA channels, weighted over bins, summed to \([B,G,C]\), and selected using each gene's strand. Only genes whose full union-exon span is contained in the input window enter the gene objective and metrics, preventing partial windows from being compared with full-gene CPM. Across the complete human 131 kb split, the maximum is 70 genes per window.

The RNA objective is log1p mean-squared error over valid gene and cell-group pairs, plus 0.1 times the standard exon pseudo-coverage loss. Gene-level metrics use the same valid mask, so padded genes and intergenic bases do not affect R2. The exon coverage term remains an auxiliary constraint on genomic localization and strand.

A one-step GPU smoke test completed with finite joint loss and validation RNA differential Pearson correlation 0.158 over four windows. A larger calibration used 512 training windows and 128 validation and test windows. Validation RNA global R2 increased from 0.126 after epoch 1 to 0.206 after epoch 4, while RNA R2 over loci increased from 0.166 to 0.259. Test RNA global R2 reached 0.199 and R2 over loci reached 0.341. Validation ATAC global R2 increased from 0.0047 to 0.0156 and ATAC R2 over loci increased from 0.047 to 0.204. These results establish that both assays learn with the revised target representation. RNA R2 over cell types remains strongly negative because many individual genes have little variance among the 60 groups; global R2, per-track R2 over genes, and double-centered correlation are more stable summaries.

## Joint primate training

The aligned pseudobulk matrices contain 53 cell groups and 13,509 one-to-one ortholog rows shared among human, macaque, and marmoset. Five aligned groups lack marmoset ATAC coverage, so paired-modality joint training uses the 48-group intersection supported by both assays in all species. The shared human Ensembl index is mapped through the provided ortholog table. Human and macaque use stable Ensembl identifiers in their GTFs. The available marmoset NCBI GTF is matched by gene symbol because it does not contain the ortholog table's Ensembl identifiers. Annotation-only checks matched 13,495 macaque genes, 99.90%, and all 13,506 uniquely mapped marmoset genes.

Each species uses its own reference genome, exon geometry, ATAC BigWigs, RNA pseudo-coverage BigWigs, and gene-expression artifact. Macaque Ensembl chromosome labels are translated to the corresponding NCBI accessions used by its FASTA and ATAC BigWigs. Species batches are emitted round-robin and training stops at the shortest species iterator each epoch, yielding equal numbers of batches per species. The output heads and LoRA plus LoCon adapters are shared. All three primates use AlphaGenome's human organism embedding because the pretrained model does not define macaque or marmoset organism indices. Consequently, the RNA head has one scaling vector; each channel's nonzero mean is pooled across species rather than inherited from human alone. Validation and test chromosome holdouts are species-specific homologous chromosome numbers.

## Metrics

Predictions and targets have shape ([B,S,C]), where (B) is the number of windows, (S) is the number of genomic positions or 128 bp bins, and (C) is the number of tracks. Differential Pearson correlation first reshapes the first two axes into observations, then subtracts the mean of each track and each observation from both matrices while adding back the grand mean. `double_centered_r2` is the square of this correlation. It measures agreement in cell-type-specific spatial variation after removing locus-wide and track-wide offsets, and it is logged for train, validation, and test splits after every epoch.

## Findings

The v0 one-step 131 kb LoRA+LoCon smoke run completed with finite joint loss 3.9817 and logged `double_centered_r2` for both heads. Best-checkpoint selection uses signed differential Pearson correlation rather than its square so a strong anticorrelation cannot be selected as an improvement.

The checked-in adapter collation now excludes the legacy `johansen_joint_lora_locon` checkpoint because it used the superseded pseudobulk matrix formed by summing per-cell-normalized expression. Corrected raw-count Johansen runs use distinct names and will re-enter the table only after producing complete epoch metrics. This prevents a known target-preprocessing defect from being presented as canonical model evidence.

At epoch 1, increasing only the outer RNA head weight from 1 to 5 improved Zemke 2023 human validation and test RNA signed double-centered correlation from 0.3963 and 0.3475 to 0.4306 and 0.3871. Validation and test ATAC decreased from 0.6760 and 0.6930 to 0.6637 and 0.6790, but the mean validation correlation increased from 0.5361 to 0.5472. The matched LoRA and LoRA plus LoCon weight-5 comparison therefore continues with early stopping. For Zemke 2024, the same epoch-1 change moved validation and test RNA from 0.3904 and 0.2319 to 0.4012 and 0.2481, while ATAC fell from 0.7306 and 0.7406 to 0.7149 and 0.7239. Mean validation decreased from 0.5605 to 0.5580, so weight 5 is not advanced for that study.

The alternate macaque Zemke 2023 fold, which holds out chromosome 10 for validation and chromosome 11 for test, reached epoch-1 ATAC correlations of 0.4857 and 0.6250 and RNA correlations of 0.2705 and 0.3799. The original chromosome 8 validation and chromosome 9 test fold reached 0.2000 and 0.5819 for ATAC and 0.2401 and 0.2863 for RNA. The large ATAC validation shift confirms that one chromosome pair is not a stable estimate of macaque performance. Adapter comparisons remain fold-matched, and aggregate performance claims require multiple chromosome folds.

A donor-level Johansen RNA reliability audit streams raw counts and aggregates a tensor with shape \([D,C,G]\), where \(D\) is the number of biological donors, \(C=47\) is the number of retained cell groups, and \(G\) is the number of modeled genes. For each group, donors are assigned greedily to two library-depth-balanced halves. Each half is summed and normalized to CPM before signed double-centered correlation. The Spearman-Brown correction estimates complete-pseudobulk reliability; under classical independent measurement error, its square root provides an assumption-based model-to-observed-target correlation ceiling. This audit is submitted for human, macaque, and marmoset and does not alter training targets.

The initial Liu LoRA plus LoCon continuation used the superseded exon-plus-gene target and required 558 BigWig reads per batch, comprising 186 measured ATAC tracks and 372 derived strand-specific exon-density tracks. It advanced only 100 of 2,619 updates in approximately 40 minutes and had not produced a checkpoint. The corrected canonical gene-only RNA objective removes the derived coverage term, retains all 186 measured ATAC targets and direct raw-count CPM gene supervision, and adds signed double-centered correlation. The obsolete process was stopped before an epoch completed. Matched LoRA, LoRA plus LoCon, and pretrained-head bootstrap comparisons now use this corrected target definition.

Gene-only manifest generation already restricts the RNA head to resolution 128, so no unused 1 bp RNA output is constructed. Directly requesting 1,024 exact 128 bp sum bins through the BigWig statistics API was tested as a possible ATAC-loading optimization. Across 30 representative 131,072 bp windows from a reconstructed Liu track, ordinary dense value reads took 0.010 seconds in total, whereas exact binned statistics took 5.56 seconds. The statistics path was therefore rejected. A future 128 bp ATAC experiment may omit the 1 bp model output, but it should continue using dense BigWig reads followed by vectorized pooling unless a precomputed compressed cache is demonstrated to be faster.

The generic multi-GPU smoke launcher initially appended `--drop-last` and then replaced the complete extra-argument array when smoke limits were enabled. Corrected Liu and human Johansen smoke arrays therefore failed the explicit even-sharding guard before constructing a batch. The launcher now applies smoke limits first and appends `--drop-last` afterward whenever more than one device is allocated. The failed jobs produced no update or metric and their dependent full runs were replaced.

The first pretrained-head bootstrap process exposed a metadata-wrapper mismatch before the initial parameter merge completed. Target registration stores each assay DataFrame inside `AlphaGenomeOutputMetadata`, while the bootstrap strand matcher expected a DataFrame directly. The initializer now unwraps the requested output type before reading strands and retains compatibility with direct DataFrames used by older callers. A regression test exercises the real wrapper type. Failed bootstrap tasks produced no update or checkpoint and were replaced after the fix.

Smoke mode now appends `_smoke` to the derived checkpoint and Weights & Biases run name in every study, species, and joint adapter launcher. This namespace is applied after any caller-provided suffix, so a technical one-step run cannot populate the metric history or optimizer state of a full-data comparison. The pending corrected Johansen smoke and full dependency chain was replaced after this change; its dataset, adapter strategies, target manifest, accelerator allocation, and dependency on corrected marmoset RNA are otherwise unchanged.

The Zemke mouse gradient diagnostic reported exactly zero output-head gradient for both ATAC and RNA while shared-adapter gradients were nonzero. Mouse-only metadata had produced one parameter row, but AlphaGenome represents mouse with organism index 1. The forward gather returned the only row for this out-of-bounds index, while its gradient scatter dropped the update, making predictions effectively independent of every output-head parameter during optimization. Both the registry and explicit predefined-head construction paths now allocate rows through the highest represented organism index, preserving one-row human-only checkpoints while assigning two rows to mouse-only heads. The invalid canonical mouse checkpoints and technical smokes were preserved outside the active checkpoint tree, pending non-held mouse objective jobs submitted from the old launcher snapshot were canceled, and all previous mouse metrics are superseded. A fixed gradient smoke must demonstrate nonzero head gradients before matched canonical training is restarted.

The alternate Zemke macaque chromosome split improved again at epoch 2. Validation and test signed double-centered $R$ reached 0.5104 and 0.6459 for ATAC and 0.3399 and 0.4325 for RNA, compared with 0.4857/0.6250 and 0.2705/0.3799 at epoch 1. This confirms that continued optimization helps both modalities, while the large difference from the chr8/chr9 canonical split remains evidence of holdout sensitivity rather than an adapter-strategy effect.

A reusable gradient-diagnostic gate validates named output heads across one or more smoke records and rejects missing, non-finite, zero, or sub-threshold head-gradient norms. The fixed mouse canonical comparison is dependency-gated on this numerical invariant rather than process exit alone.

Increasing the Mannens joint RNA outer weight from 1 to 5 did not improve the epoch-1 selection criterion. Validation RNA signed double-centered $R$ changed from 0.4361 to 0.4310 and test RNA increased from 0.5720 to 0.5976, while validation and test ATAC decreased from 0.7722/0.7830 to 0.7538/0.7645. The mean validation correlation therefore fell from 0.6042 to 0.5924. This variant is not advanced; the active gene-only factorial tests removal of synthetic exon coverage and direct correlation weighting instead.

For Zemke 2023 human, adding signed double-centered correlation weight 10 to both heads improved epoch-1 validation and test ATAC from 0.6760/0.6930 to 0.6831/0.7001 and RNA from 0.3963/0.3475 to 0.4673/0.4079. Mean validation correlation increased from 0.5361 to 0.5752, exceeding the RNA-outer-weight-5 screen at 0.5472 without an ATAC tradeoff. The partially completed outer-weight-5 continuations were stopped after preserving their epoch checkpoints. A correlation-weight-10 LoRA continuation and matched fresh LoRA plus LoCon run replace them.

Removing Mannens synthetic exon-coverage supervision without adding correlation loss left epoch-1 performance effectively unchanged. Validation and test ATAC were 0.7724 and 0.7834, while RNA was 0.4232 and 0.5714, compared with canonical values of 0.7722/0.7830 and 0.4361/0.5720. Mean validation correlation decreased from 0.6042 to 0.5978, so exon coverage is not the primary RNA bottleneck. Correlation-weighted gene-only variants remain informative because they change objective alignment rather than only input and output work.

The completed gene-only process wrote finite metrics and both checkpoints before its outer Slurm task exited with code 2. The array wrapper had invoked the generic launcher as a nested Bash process, and that launcher was edited for smoke isolation while Python was running. Bash resumed reading the modified file after Python returned and encountered a shifted byte boundary, producing a post-training unmatched-quote error. The current launcher passes `bash -n`; the pending correlation-weight 0.1 and 1 tasks were replaced so they start from the complete current file rather than remaining held by the failed array element's throttle. The scientific record is retained because training, evaluation, and checkpoint writes all completed before the wrapper-only error.

Corrected Johansen RNA aggregation was moved from the saturated general CPU queue to the I/O partition. Its first dispatch exited before Python startup because that worker did not discover the Python 3.12 shared runtime implicitly. The preprocessing launcher now exports the same runtime-library path as the model and target-assembly launchers. The failed process did not enter the aggregation script or create a partial H5AD; its smoke and full dependency chain was replaced after the environment fix.

Every v0data Slurm script that invokes a project virtual-environment Python now exports the Python 3.12 shared-runtime path explicitly. A repository test scans all nested batch scripts for this invariant, covering preprocessing, reliability, collation, target preparation, audits, and training rather than relying on worker-specific module state.

The corrected Johansen RNA targets have high donor-level reproducibility. Donor-balanced split halves were formed independently within each retained cell group, normalized to counts per million, CPM, and compared after double centering across groups and genes. Raw-CPM split-half correlations were 0.9749 for human, 0.9714 for macaque, and 0.9661 for marmoset. Spearman-Brown correction gives estimated full-data reliabilities of 0.9873, 0.9855, and 0.9828, corresponding to classical measurement-error model-correlation ceilings of 0.9936, 0.9927, and 0.9914. These are upper-bound estimates rather than model results, but they show that donor noise does not explain the current gap from a double-centered correlation of 0.8.

Gene-only RNA manifests now enforce a single reusable contract. They retain the 128 bp RNA output used for exon-weighted gene aggregation, disable coordinate-level coverage loss, attach direct gene supervision, and remove the unused 1 bp RNA output. The corrected Johansen configuration uses this contract with a double-centered correlation loss weight of one for all three species. Removing the 1 bp output avoids materializing and differentiating an output that contributes no loss or evaluation metric.

The alternate Zemke macaque chromosome fold reached its best preserved result at epoch 4. Validation and test signed double-centered $R$ were 0.5157 and 0.6356 for ATAC and 0.3597 and 0.4539 for RNA. Mean validation correlation improved from 0.4261 at epoch 3 to 0.4377 at epoch 4, although ATAC test correlation declined from 0.6523. The exploratory continuation and its not-yet-started LoRA plus LoCon partner were stopped after this checkpoint so corrected Johansen, Liu, and mouse production gates could use the available accelerator allocation.

The assay-wide pretrained-head bootstrap is not tissue matched. Its deterministic source assignment gives Mannens only 2.2% neural ATAC channels and 3.0% neural RNA channels; unrelated tissues and cell lines provide the remaining initial directions. A separate neural bootstrap identifies brain, neuronal, and macroglial target labels and restricts only those target channels to neural source metadata. Vascular, immune, microglial, and non-brain multi-organ targets retain the assay-wide source pool. Kidney and renal records containing cortex are explicitly excluded. Filtering requires at least two eligible channels for every target strand. Human pretrained RNA has diverse neural sources and is filtered, whereas human pretrained ATAC has only one neural source and therefore retains the assay-wide fallback. This variant preserves the original bootstrap as a control and tests whether semantically closer RNA initialization improves early optimization without assigning neural priors to unrelated Liu organs or non-neural brain populations.

The Mannens gene-only objective screen now includes correlation weights 0, 0.1, 1, and 10. Weight 10 is included because the completed weight-0 arm did not improve RNA and the same weight materially improved both ATAC and RNA at epoch 1 for Zemke 2023 human. This remains a separate objective screen rather than a canonical adapter comparison; it is advanced only if validation mean correlation improves without an unacceptable ATAC tradeoff.

The Mannens RNA release provides raw and normalized pseudobulk counts for 135 cell clusters but does not retain donor- or specimen-resolved counts. A technical reliability audit therefore restricts the raw matrix to the exact 134 modeled groups and 59,310 modeled genes, randomly assigns each observed molecule to one of two halves, normalizes each half to counts per million, CPM, and repeats the signed double-centered correlation calculation across random seeds. Spearman-Brown correction estimates full-depth molecule-sampling reliability. This audit can determine whether sequencing depth materially limits the target, but it cannot measure biological donor variability and must not be reported as donor reliability.

Across 20 Mannens molecule splits, raw-CPM signed double-centered $R$ was $0.99933\mathbin{\pm}0.00001$, giving Spearman-Brown full-depth technical reliability 0.99967 and an assumption-based raw-CPM model-correlation ceiling 0.99983. The corresponding log1p-CPM split-half $R$ was $0.84978\mathbin{\pm}0.00022$, with full-depth technical reliability 0.91879. The current RNA validation correlation near 0.52 is therefore not limited by molecule-sampling depth in the raw evaluation space. The larger degradation after log transformation is consistent with low-count sampling noise receiving greater influence in the baseline log-count mean-squared-error objective, so the pending raw-CPM signed-correlation screens address objective alignment rather than target rescaling.

The raw-CPM correlation objective has a minibatch-centering approximation. An audit over every native training window, including windows without genes, finds a median of 16 modeled genes per global batch of eight for Mannens and 9 for Liu. Their random batches are empty in 0.0% and 0.4% of cases. The corrected Johansen species use batches of four and contain a median of one gene; 34.9% to 38.9% of random batches have no genes, and another 26.0% to 26.9% have genes but no local double-centered variance. The instability comes from estimating one cell-group mean per track from very few genes, not from target noise.

CPM normalization provides a lower-variance alternative. Centering only across cell groups within each gene has cosine 0.99975 with the exact training-split double-centered Mannens target and retains 99.95% of its row-centered variance. The row-versus-global cosines are 0.99986 for Liu and 0.99312 to 0.99691 for corrected Johansen. A separately configured row-centered correlation loss therefore preserves the requested differential target direction while avoiding minibatch estimates of track means. Gene-aware ordering preserves every selected window exactly once and spreads gene-bearing windows across fixed-size batches. It reduces empty Johansen batches to 7.6% to 15.8%, but increases nonempty batches with no local double-centered variance to 53.0% to 58.8% because many receive only one gene. It is therefore suitable only with row-centered correlation, which is defined for one gene, and is not enabled for the pending canonical Johansen double-centered baselines. The log-count mean-squared-error term remains active to learn expression magnitude and marginal structure. This objective is screened separately and does not redefine the reported split-wide signed double-centered correlation metric.

The generic single-study and Johansen launchers expose gene-aware ordering through an explicit opt-in flag. The corrected Johansen row-objective launcher sets this flag and creates separate gene-only manifests and run names, while canonical and minibatch-double-centered jobs retain random ordering. An initial prepared manifest inherited the superseded per-cell-normalized expression paths because its launcher used the filtered pre-correction species configuration. It was never trained and is preserved outside the active output tree. The launcher now derives all three species from the corrected raw-UMI gene-only configuration; regenerated manifests explicitly replace minibatch double-centered weight one with row-centered weight ten and retain the corrected supervision paths. Liu has the same prepared objective replacement, derived from its corrected all-fragment ATAC and raw-count gene-only manifest. The Liu and Johansen screens are not submitted ahead of their canonical corrected comparisons.

Joint cross-species metrics can hide a poor native-species result. The selected LoRA and LoRA plus LoCon Zemke joint checkpoints are therefore evaluated without updates against the human, macaque, marmoset, and mouse manifests and references separately. Evaluation run names include the source epoch, and collation rejects a mismatch between that name and the checkpoint provenance recorded in `evaluation.json`.

Corrected Johansen joint checkpoints receive the same native-species treatment. Each strategy's human, macaque, and marmoset evaluation is dependency-gated on that strategy's corrected raw-count, gene-only production task and loads only the corresponding corrected best checkpoint. The legacy per-cell-normalized checkpoint is not an evaluation source.

Canonical coverage and scientific target attainment are audited separately. The target audit reports the stronger LoRA or LoRA plus LoCon strategy-selected checkpoint for each dataset and modality against validation signed double-centered correlation 0.8. It marks absent corrected runs as missing evidence rather than assigning them a low score, and it does not select separate favorable epochs for individual heads.

Primary study coverage is distinct from the larger set of canonical supplementary arms. Mannens joint, Johansen joint, Liu HDMA, Zemke 2023 joint, and Zemke 2024 are the five required non-ENCODE study comparisons. Johansen and Zemke 2023 additionally require evaluation of both joint adapter checkpoints against every native species before their comparison coverage is complete. ATAC-only Mannens, Johansen human, and individual Zemke species remain canonical supplementary analyses. Coverage completion does not imply early-stopping completion or attainment of the 0.8 correlation target.

Published-track RNA is not uniformly plateaued. Zemke 2023 human validation RNA correlation increased from 0.3963 to 0.4809 over four LoRA epochs and from 0.3999 to 0.4987 with LoRA plus LoCon. Macaque and marmoset have only one canonical epoch, so their lower correlations are undertrained observations rather than early-stopped estimates. Both macaque checkpoints retain AdamW state and are continued at low priority. The marmoset checkpoints lack optimizer state and already have user-held continuations, so no duplicate or implicit optimizer-reset run is submitted.

HDA joint LoRA completed epoch three after restarting AdamW from its epoch-two parameter-only checkpoint. The matched LoRA plus LoCon checkpoint remains at epoch two and also lacks optimizer state. Its epoch-three continuation therefore uses the same optimizer-reset boundary and stops after that one epoch, restoring a matched comparison without giving either strategy additional optimizer history.

Zemke 2024 first-batch shared-adapter gradient norms are 0.02036 for ATAC and 0.00368 for RNA. An outer RNA weight of five nearly equalizes those norms, but its epoch-one mean validation correlation decreased from 0.5605 to 0.5580 because the RNA gain did not offset the ATAC regression. A separate matched screen leaves the ATAC objective unchanged and adds signed double-centered correlation weight 10 only to RNA. This tests metric alignment rather than applying another global modality multiplier.

The HDA gene-only double-centered objective sweep uses LoRA for initial objective selection rather than spending both adapter strategies on every weight. A dependent CPU gate requires all four epoch-one records, selects by mean validation ATAC and RNA signed double-centered correlation, and advances only the strongest nonzero weight if it exceeds weight zero. The selected manifest is then run for one matched LoRA plus LoCon epoch. The separately matched row-centered screen is not included in this selection gate.

Zemke 2023 human provides direct evidence for applying the signed double-centered objective to both coordinate-resolved modalities. At epoch one, correlation weight 10 increased validation ATAC from 0.6760 to 0.6831 and RNA from 0.3963 to 0.4673 relative to the canonical LoRA run. Zemke 2024 therefore has two separately named matched screens, an RNA-only weight-10 objective and an all-head weight-10 objective. Comparing them isolates whether adding the metric-aligned ATAC term transfers the Zemke 2023 gain without obscuring the RNA-specific effect.

The pretrained neural-source audit separates metadata matching from the diversity fallback. Human AlphaGenome metadata contains 167 valid ATAC channels but only one valid neural ATAC candidate, so neural ATAC correctly retains the assay-wide pool. Human RNA has 667 valid channels and 41 neural candidates spanning positive, negative, and unstranded outputs, so neural RNA can use a restricted pool for every target representation in the current study panel. These counts are derived from released model metadata and are recorded programmatically rather than inferred from bootstrap assignments.

The same released metadata contains 305 valid human DNase channels, including 18 neural channels. AlphaGenome defines ATAC and DNase as unsquashed genome-track heads with the same 1 bp and 128 bp output architecture, and their pretrained linear weights, biases, and learned scales have matching non-channel dimensions. A separate neural-accessibility bootstrap therefore initializes target ATAC channels from neural DNase channels while retaining neural RNA initialization. This is a tissue-match screen rather than a canonical change because DNase and ATAC protocols have distinct assay-specific calibration.

Pretrained-head initialization is selected only from a complete matched matrix. The selector compares random, assay-wide bootstrap, neural RNA bootstrap, and neural DNase accessibility bootstrap at epoch one for both adapter strategies on Mannens HDA and Liu HDMA. Each initializer receives an equal average over ATAC and RNA, the two datasets, and the two strategies. A candidate advances only if its overall mean exceeds random initialization and neither modality's four-arm average regresses beyond the configured tolerance. The CPU dependency uses `afterany`, allowing failed screens to surface as explicit missing evidence rather than unresolved dependencies.

An HDA RNA-only screen tests whether the weakly aligned shared-adapter gradients still limit RNA despite their positive cosine. Removing ATAC from a randomly initialized run would change Haiku's head random-number stream and confound the comparison. The isolation screen therefore uses neural accessibility bootstrap for both the joint and RNA-only arms; pretrained RNA weights, biases, and learned scales are copied deterministically from source channels and do not depend on target-head order. The RNA objective and one-epoch data schedule remain unchanged. This is a diagnostic for modality-specific adapters or models, not a replacement for paired-modality evaluation.

The modality-isolation selector requires RNA-only improvement in the equal mean over LoRA and LoRA plus LoCon and rejects the result if either strategy regresses beyond a configured tolerance. It is run directly after both full arrays complete rather than dependency-submitted. Their full arrays are smoke-gated with `afterok`; attaching another scheduler dependency would recreate an unresolved selector if either smoke fails. A positive result motivates modality-specific adapters or separately served modality models, while a null result directs optimization back to the RNA objective and head.

The gene-level RNA target-rank audit double centers each matrix (Y\in\mathbb{R}^{G\times C}), where (G) is genes and (C) is cell groups, and computes optimal truncated singular-value approximations. In raw counts-per-million space, rank 16 gives correlation ceilings of 0.9336 for Mannens, 0.9701 for Liu, and 0.9006 to 0.9141 for corrected Johansen. Rank 8 exceeds 0.8 for Mannens, Liu, human Johansen, and macaque Johansen; marmoset requires rank 9. A rank-16 cell-group factorization therefore has enough target-side representational capacity for the requested correlation, while retaining substantial regularization relative to unconstrained channel directions. This is not a model-performance ceiling because it ignores sequence-to-latent prediction error. It motivates a factorized RNA-head screen only after the pending objective and pretrained-initialization screens establish whether the existing full head remains optimization limited.

The optional factorized RNA head replaces the organism-specific projection \(W\in\mathbb{R}^{D\times C}\) with \(P\in\mathbb{R}^{D\times R}\) and \(Q\in\mathbb{R}^{R\times C}\), where \(D\) is embedding channels, \(C\) is strand-specific RNA tracks, and \(R\) is output rank. Two mixed-precision contractions compute \(XPQ\) with preferred float32 accumulation. The inherited predefined-head contract retains the per-track bias, softplus output, learned scale, nonzero-mean unscaling, requested resolutions, strand metadata, coverage objective, and direct gene aggregation. A manifest `output_rank` selects the factorization only for RNA and its absence preserves the full projection. Rank 16 is screened first for Mannens under the unchanged canonical joint objective. Checkpoint configuration records the rank so restoration reconstructs the same head. Pretrained channel bootstrap is rejected for this variant because the released full projection does not uniquely determine factors \(P\) and \(Q\); adding a separate singular-value-decomposition initializer would be a distinct experiment.

The matched Mannens rank-16 RNA-head screen was submitted as smoke array 19794990 and dependency-gated one-epoch array 19794991. Both arrays compare LoRA with LoRA plus LoCon. The production array uses `afterok` on every smoke task, so a failed strategy cannot silently advance to the comparison run.

The reserved-head random-number-generator regression now includes the factorized projection itself. Its input factor, output factor, and bias are bit-identical between LoRA and LoRA plus LoCon initialization, as are the shared LoRA matrices and complete initial predictions. The rank-16 comparison therefore retains the matched-initialization guarantee established for full output heads.

Scientific variant collation now applies the same matched-epoch rule as the canonical adapter table. When both strategies complete a variant, the primary variant section reports their highest common epoch; independently selected checkpoints remain in a separate secondary section. This prevents unequal training duration from being mistaken for a LoRA versus LoRA plus LoCon effect in factorized-head, objective, normalization, or initialization screens.

Zemke 2023 provides raw integer gene-by-cell unique-molecular-identifier matrices alongside the published RNA BigWigs. Exact-assembly NCBI RefSeq exon annotations match 23,264 of 36,601 human features, 15,841 of 21,369 macaque features, all 22,046 marmoset features, and 26,296 of 32,285 mouse features. Gene symbols containing periods must be preserved literally; treating their suffixes as Ensembl versions would incorrectly merge distinct features. A separate direct-gene variant sums raw counts by subclass, normalizes to counts per million, and adds compact gene supervision while retaining the measured coordinate-resolved RPKM tracks and their full coverage loss. This does not replace the canonical published-track comparison.

The four-species Zemke 2023 direct-gene target build was submitted as CPU array 19796605, with two concurrent species tasks. Each task writes a species-native supervision artifact and derived manifest; no model training depends on these outputs until all channel, annotation, and CPM invariants pass.

Cross-representation agreement job 19796940 depends on successful completion of every task in array 19796605. It samples 512 genes per species, integrates the published RPKM signal over union exons, converts the integral to CPM scale, and requires raw signed double-centered correlation of at least 0.7. GPU screening remains unsubmitted until this gate passes.

The dormant downstream launcher validates that all four named species are present in the agreement artifact and exceed its recorded threshold. Only then can it submit a matched human one-epoch screen with direct RNA gene supervision and correlation weight 10, preserving the published coordinate losses. This ordering prevents a convenient but biologically conflicting target matrix from consuming GPU time.

The Zemke 2024 release contains donor-level raw 10x multiome matrices for all 40 donors and filtered-cell metadata for 295,033 cells. The metadata exactly supports 18 broad RNA subclasses and its broad donor counts reproduce Supplementary Table S2. The published 22-channel target additionally contains `Astro1_all`, `Astro2_all`, `Micro1_all`, and `Micro2_all`, but neither the metadata nor the filtered Seurat object releases cell assignments for these four subtypes. Supplementary subtype totals are not sufficient to reconstruct assignments. Direct gene supervision therefore aggregates raw unique molecular identifier counts for the 18 observed broad groups and masks the four unsupported channels rather than setting them to zero or deriving them from unrelated clustering resolutions. The published coordinate tracks remain active for all 22 channels.

Direct-gene validity now supports a vector over output groups. For target matrix (Y\in\mathbb{R}^{G\times C}), where (G) is genes and (C) is output groups, the batch mask may be (M\in\{0,1\}^{G}) for padded genes or (M\in\{0,1\}^{G\times C}) for gene and channel validity. Log-count mean-squared error, row-centered correlation, signed double-centered correlation, and accumulated evaluation statistics all exclude invalid entries. Weighted sufficient statistics reduce exactly to physically selecting valid channels when the mask is the product of gene validity and a fixed channel-validity vector. The RNA exon aggregator also accepts one unstranded channel per group, which permits direct gene supervision on the existing Zemke 2024 output head without creating a second head.

The 40-donor Zemke 2024 raw target build was submitted as job 19799531. Job 19799532 depends on its successful completion and tests agreement between direct raw-count CPM and published exon-integrated RPKM over 512 sampled genes. As for the Zemke 2023 gate, no GPU screen should consume the derived target until raw signed double-centered correlation reaches 0.7.

A separate 30-minute donor smoke, initially submitted as job 19800294, was designed to validate the smallest released donor matrix before the full build starts. It joins the 3,143 filtered `hc11` cells to raw 10x barcodes and requires exact per-group cell recovery plus audited agreement with metadata `nCount_RNA` molecule totals. This distinguishes a source-format or barcode-join defect from a failure that appears only during the all-donor aggregation.

The first partial-channel implementation exposed a coverage-metric regression when the primary Liu and Johansen jobs received accelerator slots. Coverage predictions and targets were reduced from 1 bp to 128 bp for signed double-centered statistics, but the newly generalized all-valid mask retained its 1 bp locus dimension. This produced shape mismatches before the first update in Liu jobs 19777169 and 19777170 and Johansen joint smoke 19779727. Unmasked coverage metrics now create validity directly at the reduced prediction shape; explicit direct-gene masks retain their gene-by-channel shape and are checked against the aligned prediction. Regression tests cover both one-base coverage reduction and physical channel selection. Replacement Liu smoke and full jobs are 19801463 and 19801464, corrected Johansen human jobs are 19801465 and 19801466, and corrected Johansen joint jobs are 19801467 and 19801468.

The initial Zemke 2024 CPU build and donor smoke failed before Python startup because their batch environment could not resolve `libpython3.12.so.1.0`. The three Zemke 2024 CPU scripts now export the same module Python library path as the established training launchers. Replacement all-donor build, agreement audit, and `hc11` smoke jobs are 19801460, 19801461, and 19801462. Failed dependency children were removed rather than left as `DependencyNeverSatisfied` queue entries.

The repaired metric path passed both strategies in Liu smoke 19801463 and Johansen-human smoke 19801465. Each completed one update, validation, test evaluation, and checkpoint serialization. Liu full jobs 19801464 were released and started; corrected Johansen-human full jobs 19801466 were released, and corrected Johansen-joint smoke 19801467 began as accelerator slots became available. These smoke metrics are technical initialization results over limited windows and are not included in model comparisons.

The first working-directory-corrected Zemke 2024 smoke exposed an additional source convention. Metadata cell identifiers use either `<donor>_<barcode>` or `<donor>_deep_<barcode>`, while donor 10x matrices use only `<barcode>`. Every donor has exactly one such metadata prefix, and removing all text through the final underscore produces no duplicate barcodes among 295,033 filtered cells. The shared join now implements this audited rule. The superseding all-donor build, agreement audit, and `hc11` smoke are jobs 19802600, 19802601, and 19802602.

After barcode recovery, raw 36,601-gene molecule totals exceeded metadata `nCount_RNA` because the released filtered Seurat object retains 31,485 genes. Of these, 31,476 symbols map directly and nine names ending in `.1` are R `make.unique` suffixes for duplicate raw symbols. The target builder reproduces that ordered feature union before normalization and keeps Ensembl identifiers for annotation. The resulting build, audit, and donor smoke were jobs 19803547, 19803548, and 19803549.

The first corrected Johansen joint smoke revealed numerical cancellation in split-wide gene correlation rather than a biological result. One-pass float32 sufficient statistics subtracted large row and output-group offset terms from raw CPM values and reported an impossible signed correlation of -1.2296. Gene predictions and targets are now divided by a fixed one million before accumulation, which leaves Pearson correlation invariant, and each observation is row centered before accumulation. For the rectangular validity masks used here, final subtraction of only the split-wide output-group means is algebraically equivalent to double centering while avoiding the unstable row-offset subtraction. Regression tests cover high-offset CPM, explicit channel selection, binned coverage, and exact double-centered correlation.

The partial Liu and Johansen full runs selected checkpoints with the unstable statistic and were stopped before completing an epoch. Their prior smoke and empty partial directories were preserved outside the active checkpoint tree so corrected jobs cannot append to stale `metrics.jsonl` files. Clean smoke-to-full chains are Liu 19804905 to 19804906, corrected Johansen human 19804907 to 19804908, and corrected three-species Johansen 19804909 to 19804910. The dependent full arrays require every matched LoRA and LoRA plus LoCon smoke task to complete successfully.

The SCT feature union exceeded `hc11` metadata `nCount_RNA` by 2,882 of 7,835,184 molecules, 0.0368%, but exceeded `hc1134` by 2.1608%; a universal agreement threshold on the merged feature union is therefore not defensible. Retaining all 36,601 raw Gene Expression rows exceeded `hc11` metadata by 206,261 molecules, 2.6325%. The released H5 contains only the merged SCT assay and omits the original RNA assay, so the donor-specific feature filters used to compute `nCount_RNA` cannot be recovered. The final direct-gene contract instead aggregates all raw Gene Expression rows from exactly the released cells, restricts to genes with modeled exon annotations, and normalizes that modeled matrix to CPM. This matches the corrected raw-count convention used for Liu and Johansen. Raw totals must be at least the metadata total and no more than 10% larger; the one-sided difference is a source audit, not a correction to observed molecules.

Job 19808096 recovered all 3,143 `hc11` filtered cells across 13 nonempty broad subclasses, but its SCT-union contract was superseded when the all-donor build 19808097 exposed the larger `hc1134` discrepancy. The dependent audit 19808098 was canceled. Replacement jobs use the all-raw, annotation-restricted contract above.

The all-raw `hc11` smoke passed as job 19809449. It recovered all 3,143 cells and 36,601 Gene Expression rows, with 8,041,445 raw molecules versus metadata `nCount_RNA` 7,835,184, a 2.6325% expected excess from genes omitted by the processed RNA assay. The dependency-gated all-donor build and published-track audit are jobs 19809450 and 19809451.

HDA objective workers for correlation weights 0, 0.1, and 10 began before the stable split-wide metric code was available, although their optimization objective itself was unchanged. Automatic selector job 19789513 was canceled. Metric-only array 19809946 restores all four epoch-one LoRA checkpoints and recomputes full validation and test results with the current implementation; selector 19809947 depends on every reevaluation task and accepts only `evaluation.json` with source epoch one.

Canonical and scientific-variant collation now requires every reported head to have finite signed double-centered correlation on both validation and test splits. The coverage audit applies the same predicate. A technically completed epoch with a missing split, mismatched head set, null value, or non-finite value therefore remains missing evidence and cannot create a matched LoRA versus LoRA plus LoCon result.

Corrected Johansen human epoch one provides the first matched raw-count baseline. LoRA validation signed double-centered correlation was 0.5664 for ATAC and 0.4207 for RNA, with test values 0.5978 and 0.4245. LoRA plus LoCon validation was 0.5663 and 0.4163, with test values 0.5957 and 0.4332. The mean validation values, 0.4935 and 0.4913, do not show an early LoCon advantage. Both strategies continue under matched early stopping; epoch one is below the 0.8 target for both modalities.

The corrected Zemke 2023 mouse diagnostic produced nonzero output-head gradients for both strategies. LoRA ATAC and RNA head norms were 6.7534 and 5.9988, and LoRA plus LoCon norms were 6.7756 and 6.0182. The first ad hoc CPU assertion failed before Python startup because its batch environment omitted the Python shared-library path; this was a wrapper failure, not a gradient failure. Checked-in gate job 19815004 initialized the established runtime environment and validated both diagnostic artifacts. The initially submitted CPU-gated chain was canceled before execution after the gate waited behind large preprocessing jobs; production mouse array 19815136 tasks 6 and 7 was submitted after the successful assertion. These production runs supersede every mouse run made before the organism-row fix.

The Zemke 2023 human correlation-weight-10 screen continues to improve at epoch two for LoRA. Validation signed double-centered correlation reached 0.6949 for ATAC and 0.4835 for RNA, compared with 0.6831 and 0.4673 at epoch one. Test values were 0.7183 and 0.4052. LoRA plus LoCon has completed only epoch one, with validation ATAC and RNA values 0.6822 and 0.4865, so a strategy comparison beyond epoch one remains premature.

The Mannens gene-only correlation-weight-1 LoRA arm completed epoch one. Its validation ATAC and RNA correlations were 0.7649 and 0.4253, with test values 0.7787 and 0.6420. These values are included in the scientific-variant snapshot but remain preliminary for objective selection until the dependency-gated evaluator recomputes weights 0, 0.1, 1, and 10 from their epoch-one checkpoints under the same current metric implementation.

The Mannens row-centered correlation-weight-10 LoRA screen did not improve objective alignment. Validation ATAC and RNA correlations were 0.7398 and 0.3709, with test values 0.7512 and 0.4943. Both modalities regressed relative to the weight-0 gene-only screen, whose validation values were 0.7724 and 0.4232. Because Mannens random batches already contain a median of 16 modeled genes and the weight-1 double-centered screen also showed no material validation gain, the just-started row-centered LoRA plus LoCon counterpart was canceled after 66 seconds. Its empty checkpoint directory was preserved outside the active checkpoint tree. Lower row-centered weights are not advanced without evidence of a beneficial objective direction.

The original Zemke joint native-evaluation arrays rebuilt each native species as an independent model. Human, macaque, and marmoset therefore allocated one organism row and failed strict restoration of the two-row joint output-head tensors; mouse allocated two rows and did not expose the mismatch. The checkpoint is not malformed, and truncating it would discard the organism contract. The corrected launcher reconstructs the original joint species configuration for every evaluation and passes `--evaluate-species` only to select the native data module. Obsolete arrays 19787055 and 19787056 were canceled, and replacement LoRA and LoRA plus LoCon evaluations for human, macaque, marmoset, and mouse are jobs 19815320 through 19815327.

Corrected Liu HDMA epoch one completes the primary study's first matched comparison. LoRA validation ATAC and RNA correlations were 0.7613 and 0.5231, with test values 0.7791 and 0.4445. LoRA plus LoCon validation values were 0.7629 and 0.5159, with test values 0.7795 and 0.4437. The corrected raw-count, gene-only LoRA RNA value exceeds the legacy exon-plus-gene validation value 0.4053 by 0.1178, while ATAC remains comparable. LoCon has no epoch-one advantage. Both strategies continue under early stopping, and neither modality has yet reached validation correlation 0.8.

Corrected Johansen human epoch two shows continued RNA learning rather than an epoch-one plateau. LoRA validation ATAC and RNA correlations reached 0.5818 and 0.5144, increases of 0.0154 and 0.0936 from epoch one; test values were 0.6140 and 0.4543. LoRA plus LoCon validation values were 0.5786 and 0.4897, with test values 0.6111 and 0.4688. LoCon's test RNA is 0.0146 higher, but its validation ATAC and RNA are both lower, so it does not improve the selection criterion at the highest matched epoch. Both arms continue in epoch three.

Zemke 2023 human correlation weight 10 produces a validation RNA gain without an ATAC tradeoff by LoRA epoch three. Validation ATAC and RNA correlations were 0.6942 and 0.5414, with test values 0.7174 and 0.4311. Canonical LoRA epoch four reached 0.6957 and 0.4809 on validation, so the objective increases RNA by 0.0605 while preserving ATAC. At matched epoch two, LoRA and LoRA plus LoCon RNA values were 0.4835 and 0.4832; the apparent epoch-three LoRA advantage cannot be attributed to strategy until LoRA plus LoCon completes the same epoch.

The direct-gene Zemke builders were moved from the congested general CPU queue to the dedicated input/output partition because both stream released sparse matrices and genomic tracks. The Zemke 2023 launcher now initializes the Python shared-library path used by all working batch launchers. Its first replacement array resolved the script-relative working directory to Slurm's spool location and failed before Python work; the dependent audit was canceled. Both Zemke 2023 scripts now use the submission directory contract. Zemke 2024 build 19815654 and audit 19815655 use the previously validated all-raw, annotation-restricted contract.

The corrected Zemke 2023 builder then exposed a source-resolution mismatch shared by all four species. Each released matrix contains `L4/5 IT` and chandelier cells but no `L5 IT` observations, while the 20-track panel contains separate `L4_5_IT` and `L5_IT` tracks and excludes chandelier cells. The H5AD observation tables retain only cell identifier, sample, and subclass, so the finer `L5_IT` assignment cannot be recovered from the released count matrices. Direct-gene artifacts now preserve all 20 channels, assign counts to the 19 supported published groups, and store `group_valid=false` for `L5_IT`; coordinate-level RPKM supervision remains active for that channel. Losses, training metrics, and the track-agreement audit exclude only the invalid direct-gene entries. Replacement build 19816167 runs two species at a time and audit 19816168 depends on all four.

All four direct-count artifacts built successfully, but the raw compatibility gate failed. Raw CPM versus exon-integrated published RPKM double-centered correlation was 0.5882 for human, 0.3777 for macaque, 0.4814 for marmoset, and 0.4377 for mouse, below the preregistered 0.7 threshold. Log1p double-centered correlation was 0.8462, 0.8347, 0.8507, and 0.8705. After centering and unit-normalizing each gene across cell groups, mean within-gene correlation was 0.7706, 0.7167, 0.7326, and 0.7313, with medians 0.8828, 0.8360, 0.8657, and 0.8652. Thus cell-group patterns agree, but UMI CPM and coverage-derived RPKM differ in gene-specific raw scale. The raw-count correlation-weight-10 screen is not advanced. A published-track-integrated direct-gene objective is the appropriate next test because it can focus optimization on gene totals without changing the evaluated target scale.

The published-track direct-gene preparation integrates every released RNA RPKM track over the same native-assembly union exons used for model predictions and renormalizes modeled gene totals to CPM. It retains all 20 published groups, coordinate coverage weight one, direct log-count weight one, and correlation weight 10. Four-species preparation array 19817129 is running two species at a time. Human smoke array 19817393 depends on the human artifact, one-epoch array 19817394 depends on both smoke strategies, and coordinate-only evaluations 19817395 and 19817396 depend on their corresponding full checkpoints. A useful result must improve the direct gene metric without degrading the original published coordinate metric.

The Zemke 2024 all-donor build completed the same raw-target gate over its 18 supported groups. Raw CPM versus exon-integrated RPKM double-centered correlation was 0.3292, while log1p correlation was 0.8666; raw and integrated nonzero fractions were 0.8290 and 0.8314. The discrepancy is therefore not explained by sparsity and matches the gene-specific scale mismatch observed in Zemke 2023. The raw-count direct-gene launcher remains dormant because the preregistered 0.7 threshold failed. A published-track-integrated variant is deferred until the human Zemke 2023 experiment demonstrates benefit on both its direct and canonical coordinate evaluations.

Zemke 2023 human correlation-weight-10 LoRA improved again at epoch four, reaching validation ATAC and RNA correlations 0.6994 and 0.5704 and test values 0.7220 and 0.4399. Relative to the stronger canonical strategy-selected checkpoint, validation RNA improves by 0.0716 from 0.4987 and ATAC improves by 0.0023 from 0.6971. The objective gain is now clear for LoRA, but LoRA plus LoCon has completed only epoch two and must reach the same epoch before adapter strategy is compared. Corrected Johansen human LoRA validation RNA declined from 0.5144 at epoch two to 0.4908 at epoch three while ATAC increased from 0.5818 to 0.5862, reducing the mean selection criterion; the epoch-two checkpoint remains best and training continues under patience-based early stopping.

At matched Zemke 2023 human correlation-weight-10 epoch three, LoRA validation ATAC and RNA correlations were 0.6942 and 0.5414 and LoRA plus LoCon values were 0.6931 and 0.5430. Their mean validation criteria, 0.6178 and 0.6181, are effectively tied. LoRA plus LoCon test RNA was higher, 0.4555 versus 0.4311, but this does not establish a validation-selected strategy advantage. The objective gain is therefore shared by both adapter strategies.

Corrected Johansen human matched epoch three now favors LoRA plus LoCon. LoRA validation ATAC and RNA correlations were 0.5862 and 0.4908, whereas LoRA plus LoCon reached 0.5886 and 0.5237; test values were 0.6181/0.4692 and 0.6218/0.4588. LoRA plus LoCon's validation mean 0.5562 also slightly exceeds LoRA's independently selected epoch-two mean 0.5481. The joint three-species LoRA arm completed epoch one at only 0.5270 ATAC and 0.2406 RNA on validation, with test values 0.4840 and 0.3564. It continues training, and the still-running LoRA plus LoCon epoch is required before deciding whether this reflects slower joint convergence or negative transfer.

Corrected Zemke 2023 mouse LoRA epoch one reached validation ATAC and RNA correlations 0.6641 and 0.2095 and test values 0.6642 and 0.2310. This is the first valid mouse-only result after the organism-row correction. The LoRA plus LoCon arm remains pending, so the mouse result is reported only as independently selected evidence and not as an adapter comparison.

Mannens HDA joint LoRA plus LoCon reached epoch three, completing the matched comparison at the same depth as LoRA. Validation ATAC and RNA correlations were 0.8035 and 0.5190 for LoRA plus LoCon, compared with 0.7947 and 0.5170 for LoRA. Test values were 0.8127 and 0.6089 for LoRA plus LoCon and 0.8061 and 0.6181 for LoRA. LoRA plus LoCon therefore reaches the 0.8 validation target for ATAC and improves the mean validation selection criterion by 0.0053, while neither strategy approaches the RNA target and LoRA retains a 0.0092 test RNA advantage.

GPU scheduling now preserves matched evidence at epoch boundaries. The corrected Zemke 2023 mouse LoRA and corrected joint Johansen LoRA processes were stopped during epoch two after verifying complete epoch-one parameter and AdamW optimizer checkpoints. Their partial epoch-two updates were intentionally discarded. This released capacity for the missing mouse LoRA plus LoCon arm and HDA objective reevaluation. Both LoRA trajectories can resume exactly from their persisted epoch-one states after their counterparts provide matched evidence. The continuation helpers require a completed source epoch and optimizer state, and the Johansen helper additionally requires the corrected species manifest. Mouse LoRA continuation 19818654 depends on the active mouse LoRA plus LoCon process, while Johansen joint LoRA continuation 19818655 depends on the active joint LoRA plus LoCon process. Obsolete held mouse continuations were canceled so they cannot duplicate these dependency-controlled runs.

The two held Zemke 2023 marmoset continuations restore matched canonical epoch-one parameter checkpoints that both lack optimizer state. Jobs 19750765 and 19750766 were released together at low priority. Both strategies therefore restart AdamW at the same epoch boundary, as in the matched Mannens epoch-three continuation, and neither receives an optimizer-history advantage. The jobs continue under the canonical early-stopping contract.

Zemke 2023 human correlation-weight-10 LoRA was paused during epoch five after confirming a complete epoch-four parameter and AdamW checkpoint. Its partial epoch-five updates were discarded while LoRA plus LoCon completes epoch four. This makes accelerator capacity available to the published-gene objective smoke without weakening the highest currently matched strategy comparison, and the LoRA trajectory remains exactly resumable.

Published-track-derived Zemke 2023 direct-gene artifacts completed for human, macaque, marmoset, and mouse. Their matrices contain 23,264, 15,841, 22,046, and 26,291 genes by 20 groups, respectively. Every group is valid, every value is finite, and each group sums to one million counts per million within float32 tolerance. These checks establish target integrity but not objective benefit; the dependency-gated human LoRA and LoRA plus LoCon smoke remains required before full training.

Corrected Johansen human LoRA completed epoch four with validation ATAC and RNA correlations 0.5904 and 0.4647, reducing its mean selection criterion from the epoch-two best of 0.5481 to 0.5276. The run was paused after 200 updates of epoch five, with its complete epoch-four parameter and AdamW state retained. Continuation 19819054 depends on completion of the active LoRA plus LoCon process, which was near the end of epoch four when the dependency was submitted. This preserves matched training depth while making the unmatched slot available to missing objective evidence.

At matched corrected Johansen human epoch four, LoRA plus LoCon reached validation ATAC and RNA correlations 0.5931 and 0.5067, compared with 0.5904 and 0.4647 for LoRA. Test values were 0.6233 and 0.4747 for LoRA plus LoCon and 0.6216 and 0.4643 for LoRA. Both strategies declined from their independently selected checkpoints, but LoRA plus LoCon retains the stronger best validation mean, 0.5562 at epoch three versus 0.5481 for LoRA at epoch two.

At matched Zemke 2023 human correlation-weight-10 epoch four, LoRA validation ATAC and RNA correlations were 0.6994 and 0.5704, while LoRA plus LoCon reached 0.6998 and 0.5298. Test values were 0.7220 and 0.4399 for LoRA and 0.7217 and 0.4452 for LoRA plus LoCon. The strategies remain tied for ATAC, but LoRA has a 0.0405 validation RNA advantage and the stronger mean selection criterion at this depth. LoRA plus LoCon was paused after 100 epoch-five updates with its epoch-four parameter and AdamW state retained, matching the paused LoRA boundary and releasing capacity for the published-gene screen.

The metric-consistent Mannens HDA gene-only objective sweep selected signed double-centered correlation weight 0.1. Validation ATAC and RNA correlations were 0.7710 and 0.4294, giving mean 0.6002 compared with 0.5978 for weight zero. Weights 1 and 10 reduced the mean to 0.5951 and 0.5680. The 0.0023 improvement is modest, so the selected weight advances only to the preregistered matched LoRA plus LoCon epoch-one test and does not replace the canonical objective. The dependency-gated CPU selector remained pending after all four evaluations completed, so its deterministic selection artifact was generated directly and matched LoRA plus LoCon job 19821579 was submitted for weight 0.1. The redundant selector was canceled before execution to prevent duplicate submission.

Corrected Liu HDMA matched epoch two favors LoRA plus LoCon at that depth. LoRA validation ATAC and RNA correlations were 0.7671 and 0.4481, while LoRA plus LoCon reached 0.7680 and 0.4930. Test values were 0.7842 and 0.4718 for LoRA and 0.7845 and 0.4862 for LoRA plus LoCon. Both validation means declined from epoch one, so their independently selected checkpoints remain at epoch one, but LoRA plus LoCon has a 0.0229 mean advantage at matched epoch two. Both runs continue under the same patience-based early-stopping rule.

Corrected Zemke 2023 mouse epoch one completes the first valid matched strategy comparison. LoRA plus LoCon reached validation ATAC and RNA correlations 0.6733 and 0.2141, modest improvements over LoRA values 0.6641 and 0.2095. Test values were 0.6719 and 0.2403 for LoRA plus LoCon and 0.6642 and 0.2310 for LoRA. The LoRA plus LoCon process was stopped after its first epoch-two update, with its complete epoch-one parameter and AdamW state retained. Explicit continuation 19822553 advances LoRA to epoch two, and dependency-gated continuation 19822554 then advances LoRA plus LoCon to the same depth. These bounded continuations preserve optimizer history and prevent one strategy from running multiple epochs ahead; later continuations still use patience-based early stopping.

The JAX trainer now optionally accepts a linear-warmup cosine-decay learning-rate schedule while retaining constant learning rate as the canonical default. For peak rate \(\eta\), total updates \(T\), warmup updates \(W\), and final ratio \(q\), the schedule increases from zero to \(\eta\) over \(W\) updates and decays by a half cosine to \(q\eta\) at update \(T\). This targets the recurrent pattern in which RNA validation correlation peaks during the first few epochs and then regresses under constant \(10^{-3}\) AdamW. HDA LoRA smoke 19826928 and dependency-gated eight-epoch screen 19826929 use peak \(3\times10^{-4}\), 262 warmup updates, final ratio 0.1, and the unchanged canonical targets, adapter rank, and selection metric. The screen must improve mean validation correlation before a matched LoRA plus LoCon arm is submitted.

Corrected three-species Johansen joint epoch one favors LoRA plus LoCon. Validation ATAC and RNA correlations were 0.5339 and 0.2667 for LoRA plus LoCon, compared with 0.5270 and 0.2406 for LoRA. Test values were 0.4926 and 0.3933 for LoRA plus LoCon and 0.4840 and 0.3564 for LoRA. The LoRA plus LoCon process was stopped after 1,200 epoch-two updates, preserving its complete epoch-one parameter and AdamW checkpoint. Explicit continuation 19828490 advances LoRA to epoch two before dependency-gated continuation 19828491 advances LoRA plus LoCon, restoring bounded matched training depth.

The Zemke 2024 RNA-only correlation-weight-10 screen completed one matched epoch. LoRA validation ATAC and RNA correlations were 0.7073 and 0.4916, while LoRA plus LoCon reached 0.7079 and 0.4800. Canonical strategy-selected values are 0.7499 ATAC and 0.4759 RNA. The RNA-only objective therefore gives LoRA a 0.0157 RNA gain but an ATAC regression of 0.0426, lowering the validation mean by 0.0134; LoRA plus LoCon does not improve RNA. This variant is not advanced as a replacement objective.

Both corrected Johansen joint epoch-one checkpoints now exist, so native-species evaluation no longer needs to depend on a live training array. The evaluator accepts an optional source dependency but requires each materialized `best` checkpoint before submission. Human, macaque, and marmoset evaluations for LoRA and LoRA plus LoCon are jobs 19829751 through 19829756. A repeated launcher invocation also submitted jobs 19829938 through 19829943; those duplicates were canceled before execution. The six retained results are required before the primary Johansen comparison has complete native-species coverage.

Corrected Johansen human LoRA plus LoCon completed epoch five with validation ATAC and RNA correlations 0.5930 and 0.4367. Its mean 0.5149 is below the epoch-three best 0.5562 and below epoch four 0.5499, confirming continued RNA regression under constant \(10^{-3}\) AdamW. The process was stopped after 100 epoch-six updates, retaining its complete epoch-five parameter and optimizer state. The dependent LoRA continuation was canceled because both strategies already have matched epoch-four evidence and further constant-rate updates are not supported by validation. Lower-rate results will determine whether either trajectory should resume.

Row-centered RNA correlation sweeps now compare weights 0, 0.1, 1, and 10 under identical gene-balanced ordering for corrected Liu and joint Johansen LoRA. Weight zero is required because balancing changes minibatch composition independently of the correlation term. All derived manifests set the minibatch double-centered term to zero and vary only the row-centered weight. Smoke jobs 19833806, 19833810, 19833814, and 19833818 gate Liu production jobs 19833807, 19833811, 19833815, and 19833819. Johansen smoke jobs 19833808, 19833812, 19833816, and 19833820 gate production jobs 19833809, 19833813, 19833817, and 19833821. An interrupted first launcher attempt submitted Liu weight-zero smoke 19832197 and dependent full job 19832198; both were canceled before execution, including removal of the expected unsatisfied dependency. That background process later completed the remaining submissions as jobs 19832424 through 19833594; all 14 overlapping smoke/full jobs were also canceled before execution, leaving only the complete documented matrix. Only a LoRA weight that improves mean validation correlation over the matched weight-zero ordering control advances to LoRA plus LoCon.

The Zemke 2023 published-track-derived gene objective failed its direct epoch-one gate. LoRA validation ATAC and RNA correlations were 0.6325 and 0.3533, while LoRA plus LoCon reached 0.5988 and 0.2498. These are both below the existing correlation-weight-10 epoch-one results, 0.6831/0.4673 and 0.6822/0.4865. The direct-gene objective is therefore not advanced. Dependency-released coordinate-only evaluations 19817395 and 19817396 remain required to measure whether it also degraded the original published-track RNA metric.

The selected Mannens HDA gene-only correlation-weight-0.1 objective completed its matched epoch-one test. LoRA plus LoCon validation ATAC and RNA correlations were 0.7755 and 0.4282, compared with 0.7710 and 0.4294 for LoRA. Their means, 0.6018 and 0.6002, are both below canonical epoch-one means 0.6021 and 0.6042, respectively. The small LoCon strategy gain does not overcome the objective regression, so weight 0.1 does not replace the canonical HDA loss and is not continued.

Corrected Zemke 2023 mouse LoRA epoch two improved validation ATAC and RNA correlations to 0.6886 and 0.2448 from 0.6641 and 0.2095 at epoch one; test values reached 0.6871 and 0.3019. The matched LoRA plus LoCon epoch-two continuation remains pending. Mouse RNA is learning under the corrected organism path but remains far below the 0.8 target.

Zemke 2023 macaque LoRA epoch two improved validation ATAC and RNA correlations to 0.2603 and 0.2851 from 0.2000 and 0.2401 at epoch one; test values reached 0.6016 and 0.3004. The open-ended process was stopped after its first epoch-three update, retaining the complete epoch-two parameter and AdamW state. Its pending open-ended LoRA plus LoCon continuation was replaced by bounded epoch-two job 19839935. Later depth is considered only after the matched epoch-two comparison.

Corrected Liu HDMA reached matched epoch three with a substantial validation RNA recovery. LoRA validation ATAC and RNA signed double-centered correlations were 0.7709 and 0.6481, compared with 0.7723 and 0.5561 for LoRA plus LoCon. Test values were 0.7887/0.4886 for LoRA and 0.7895/0.4700 for LoRA plus LoCon. Mean validation correlation improved from 0.6422 at LoRA epoch one to 0.7095 at epoch three and from 0.6394 to 0.6642 for LoRA plus LoCon. Both runs therefore continue under early stopping; LoRA currently has the stronger RNA trajectory, while LoCon retains a small ATAC advantage.
